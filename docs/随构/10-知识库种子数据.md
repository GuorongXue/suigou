---
tags: [随构, 知识库, 种子数据]
created: 2026-08-04
status: 初稿-待行家评审
---

# 随构 · 知识库种子数据（v0.1）

> Schema 见 [[09-知识库Schema设计]]。数据来源标注于各条 meta；`inferred` 级条目**必须**通过行家评审才能升级为 `verified`。
> 价格快照日期：2026-08-04（源：嘉立创型材库/清单页截图）。

## 1. 截面库（3条种子）

```yaml
# sections/eu-2020.yaml
meta: { id: eu-2020, source: jlcfa-library-screenshot, confidence: public, review: pending, version: 0.1.0, updated: 2026-08-04 }
section:
  id: eu-2020
  name: 2020铝型材 欧标
  standard: eu
  series: 20
  size: [20, 20]
  slot: { type: T, width: 6 }
  wallThickness: 1.4
  weightPerMeter: 0.48
  coreHole: { diameter: 5.0, tapping: M6, tapDepth: 15 }   # 源:工艺页(欧标20系列)
  faces:
    - { index: 0, normal: [ 1, 0, 0], slots: [{ offset: 0, width: 6 }] }
    - { index: 1, normal: [-1, 0, 0], slots: [{ offset: 0, width: 6 }] }
    - { index: 2, normal: [ 0, 1, 0], slots: [{ offset: 0, width: 6 }] }
    - { index: 3, normal: [ 0,-1, 0], slots: [{ offset: 0, width: 6 }] }
  outlineRef: dxf/eu-2020.dxf   # 待描摹
  price: { perMeter: 13.30, currency: CNY, tax: false }
  mechanics:   # ⚠ inferred，行家评审门禁项
    momentOfInertia: { ix: 7.0e3, iy: 7.0e3 }   # mm^4，手册近似值
    elasticModulus: 6.9e4                        # MPa 6063-T5

# sections/eu-3030.yaml
meta: { id: eu-3030, source: jlcfa-library-screenshot, confidence: public, review: pending, version: 0.1.0, updated: 2026-08-04 }
section:
  id: eu-3030
  name: 3030铝型材 欧标
  standard: eu
  series: 30
  size: [30, 30]
  slot: { type: T, width: 8 }
  wallThickness: 2.0
  weightPerMeter: 0.85
  coreHole: { diameter: 6.8, tapping: M8, tapDepth: 20 }   # 源:工艺页(欧标30系列 6.8/7→M8/20mm)
  faces:
    - { index: 0, normal: [ 1, 0, 0], slots: [{ offset: 0, width: 8 }] }
    - { index: 1, normal: [-1, 0, 0], slots: [{ offset: 0, width: 8 }] }
    - { index: 2, normal: [ 0, 1, 0], slots: [{ offset: 0, width: 8 }] }
    - { index: 3, normal: [ 0,-1, 0], slots: [{ offset: 0, width: 8 }] }
  outlineRef: dxf/eu-3030.dxf
  price: { perMeter: 23.62, currency: CNY, tax: false }    # 与清单页反推一致(123mm→3.01✓)
  mechanics:   # ⚠ inferred
    momentOfInertia: { ix: 3.3e4, iy: 3.3e4 }
    elasticModulus: 6.9e4

# sections/eu-4040-s8.yaml
meta: { id: eu-4040-s8, source: jlcfa-process-page, confidence: public, review: pending, version: 0.1.0, updated: 2026-08-04 }
section:
  id: eu-4040-s8
  name: 4040铝型材 欧标 槽8
  standard: eu
  series: 40
  size: [40, 40]
  slot: { type: T, width: 8 }
  wallThickness: null          # ⚠ 未采集，待补
  weightPerMeter: null         # ⚠ 未采集，待补
  coreHole: { diameter: 6.8, tapping: M8, tapDepth: 20 }   # 源:工艺页(欧标40槽8同30系列)
  faces:
    - { index: 0, normal: [ 1, 0, 0], slots: [{ offset: 0, width: 8 }] }
    - { index: 1, normal: [-1, 0, 0], slots: [{ offset: 0, width: 8 }] }
    - { index: 2, normal: [ 0, 1, 0], slots: [{ offset: 0, width: 8 }] }
    - { index: 3, normal: [ 0,-1, 0], slots: [{ offset: 0, width: 8 }] }
  outlineRef: dxf/eu-4040-s8.dxf
  price: { perMeter: null }    # ⚠ 待补
  mechanics:   # ⚠ inferred
    momentOfInertia: { ix: 8.8e4, iy: 8.8e4 }
    elasticModulus: 6.9e4
```
```yaml
# —— v0.2 力学参数补充（2026-08-04 行家评审第三部分：默认值verified，架构改为厂家分库）
# ⚠ 关键认知：惯性矩无统一值，厂家间差20%~50%（槽型/壁厚/中心孔/加强筋）。默认值仅供估算，正式设计用具体厂家数据。

materials:
  al-6063-t5: { elasticModulus: 6.9e4, density: 2700, poisson: 0.33, confidence: verified }  # MPa / kg·m⁻³

sectionMechanics:           # 架构：默认值 + 厂家覆盖（行家建议，用户可选厂家自动切换）
  defaults:                 # verified as 典型默认值
    eu-2020: { ix: 0.7e4, range: [0.6e4, 0.8e4] }    # mm⁴（0.7 cm⁴）
    eu-3030: { ix: 3.3e4, range: [3.0e4, 3.8e4] }
    eu-4040-s8: { ix: 8.8e4, range: [8.0e4, 10e4], note: 重型系列可超10 cm⁴ }
  brandOverrides:           # 待填：Bosch Rexroth / item / MISUMI / 80/20 / Futura 官方型录（A/Ix/Iy/Wx/Wy/rx/ry）
    {}

dataSourceTiers:            # meta.source 分级标准（行家给出）
  tier1-5star: [厂商CAD, 厂商Catalog]        # FEA验证，可信度最高
  tier2-4star: [机械设计手册, Roark's, Shigley's]   # 只用于公式，不用于型材参数
  tier3-3star: [网络转载, 淘宝, 论坛]         # 禁止入库
```

## 2. 连接件库（4条种子）

```yaml
# connectors/corner-bracket-30.yaml
meta: { id: corner-bracket-30, source: jlcfa-designer, confidence: public, review: pending, version: 0.1.0 }
connector:
  id: corner-bracket-30
  name: 角码 30系列(面型)
  category: corner
  visibility: external
  compatible: { slotWidths: [8], series: [eu-3030, eu-4040-s8] }
  strengthClass: 2             # ⚠ inferred 分级待评审
  removable: true
  lengthOffset: 0              # 角码不占端面，下料长度无修正
  attach: { facesRequired: 2, orientation: perpendicular }
  machining: []                # 免加工
  bom:
    - { sku: t-nut-m6, qty: 2 }
    - { sku: bolt-m6-l12, qty: 2 }

# connectors/internal-30.yaml
meta: { id: internal-30, source: jlcfa-designer, confidence: public, review: pending, version: 0.1.0 }
connector:
  id: internal-30
  name: 内置连接件 30系列
  category: internal
  visibility: hidden
  compatible: { slotWidths: [8], series: [eu-3030] }
  strengthClass: 3             # ⚠ inferred
  removable: true
  lengthOffset: 0
  attach: { facesRequired: 2, orientation: perpendicular }
  machining:
    - { type: wrench-hole, diameter: 8.5, onMember: self, note: 扳手操作孔(源:工艺页30系列欧标8.5) }
  bom:
    - { sku: internal-conn-30, qty: 1 }

# connectors/anchor-30.yaml
meta: { id: anchor-30, source: jlcfa-process-page, confidence: public, review: pending, version: 0.1.0 }
connector:
  id: anchor-30
  name: 锚式连接件 30系列
  category: anchor
  visibility: hidden
  compatible: { slotWidths: [8], series: [eu-3030] }
  strengthClass: 4             # ⚠ inferred
  removable: true
  lengthOffset: 0
  attach: { facesRequired: 2, orientation: perpendicular }
  machining:
    - type: through-hole
      diameter: 11.5
      onMember: adjacent
      offsetFromEnd: "19 - slotWallThickness + 2"   # G=19-T+2(源:工艺页)
  bom:
    - { sku: tpef-308-0, qty: 1 }

# connectors/screw-joint-30.yaml（打孔攻丝连接）
meta: { id: screw-joint-30, source: jlcfa-order-page, confidence: public, review: pending, version: 0.1.0 }
connector:
  id: screw-joint-30
  name: 打孔攻丝连接 30系列(M8圆柱头)
  category: tap
  visibility: hidden
  compatible: { slotWidths: [8], series: [eu-3030] }
  strengthClass: 4             # ⚠ inferred
  removable: true
  lengthOffset: 0
  attach: { facesRequired: 1, orientation: perpendicular, usesCoreHole: true }
  machining:
    - { type: end-tap, thread: M8, depth: 20, onMember: target }               # 被连接件端面攻丝
    - { type: counterbore, d: 9, D: 14, depth: 8.5, onMember: through }        # 穿过件沉头孔(源:工艺页欧标30 Z8)
  bom:
    - { sku: edla-s1-m8-l40, qty: 1, priceUntaxed: 0.5646 }   # 源:配件清单页
```

```yaml
# connectors/*.yaml —— v0.2（2026-08-04 行家评审第二部分：strengthClass 全部verified并修正排序）
# 排序依据（行家原话）：端面攻丝=轴向预紧充分利用端面受力 > 锚式=内部锁紧稳定 > 角码=刚度不错但依赖自身与螺栓 > 内置=优势是美观不是承载

- connector: { id: screw-joint-30,    strengthClass: 5, loadRole: primary,    confidence: verified }
- connector: { id: anchor-30,         strengthClass: 4, loadRole: primary,    confidence: verified }
- connector: { id: corner-bracket-30, strengthClass: 3, loadRole: primary,    confidence: verified }
- connector: { id: internal-30,       strengthClass: 2, loadRole: positioning-aesthetic,   # 关键修正：原误标3级高于角码
      note: 定位与外观用途，长期振动下易松动，禁止单独主承重, confidence: verified }
# 其余字段（machining/bom/attach等）同v0.1不变
```

## 3. 规则包种子

```yaml
# rules/selection.yaml —— v0.2（2026-08-04 行家评审第一部分通过，阈值升级 verified）
loadModel:
  loadType: [distributed, concentrated]
  # 行家：集中载荷挠度显著大于均布；规则默认按均布评估，集中载荷时跨度降一档
rules:
  - { id: sel-001,  when: { span: "<=600",  loadKg: "<=20", loadType: distributed }, use: eu-2020,
      rationale: 2020定位轻型框架/护罩/电子设备，不承担较大结构载荷（行家原话）, confidence: verified }
  - { id: sel-001b, when: { span: "<=800",  loadKg: "<=10", loadType: distributed }, use: eu-2020, confidence: verified }
  - { id: sel-002,  when: { span: "<=1000", loadKg: "<=50", loadType: distributed }, use: eu-3030,
      rationale: 行家闭眼边界1000@50均布, confidence: verified }
  - { id: sel-002b, when: { span: "<=800",  loadKg: "<=80", loadType: distributed }, use: eu-3030, confidence: verified }
  - { id: sel-003,  when: { anyOf: { span: ">1200", loadKg: ">80", scene: vibration, need: precision } }, use: eu-4040-s8,
      rationale: 4040核心优势是刚度更大挠度更小（非仅强度）, confidence: verified }
  - { id: sel-005,  when: { loadType: concentrated }, action: downgradeSpanOneLevel,
      rationale: 集中载荷挠度约为均布的显著倍数, confidence: verified }
  - { id: sel-006,  type: post-selection,
      rule: "短立柱可比横梁小一级；高立柱不可简单降级（校核Euler屈曲而非弯曲）",
      heightThreshold: 800   # ⚠ inferred 高度分界待二次确认
      confidence: partially-verified }
  - { id: sel-004,  when: { scene: [aquarium, child, overhead] }, action: upgradeOneSeries, rationale: 高风险场景升一级 }
conflicts: { priority: [safety, budget, aesthetics] }

# rules/connection.yaml（未变，待第二部分评审）
rules:
  - { id: con-001, when: { joint: corner-90, load: light, hiddenRequired: false }, use: corner-bracket-30, rationale: 便宜免加工可拆 }
  - { id: con-002, when: { joint: corner-90, hiddenRequired: true, load: "light|medium" }, use: internal-30, rationale: 外观优先,代价加工费 }
  - { id: con-003, when: { joint: corner-90, load: heavy }, use: anchor-30, rationale: 强度优先隐藏式 }
  - { id: con-004, when: { joint: end-to-face, alignedCoreHole: true }, use: screw-joint-30, rationale: 端面对接最简加工路径 }

# rules/validation.yaml —— v0.2（val-003 行家修正）
rules:
  - { id: val-001, type: max-span, expr: "span <= verified阈值(见selection)", onFail: reject-and-ask, confidence: verified }
  - { id: val-002, type: deflection, expr: "5*q*L^4/(384*E*I) <= L/300", onFail: suggest }   # ⚠ 判据待第三部分评审
  - { id: val-003, type: high-risk-scene, when: { scene: [aquarium, child, overhead] },
      action: { addSafetyFactor: 2.0, deflectionCheckMandatory: true, showWarning: true },
      rationale: 行家：人身安全结构安全系数≥2.0且必须控制挠度（不能只满足强度）, confidence: verified }
  - { id: val-004, type: post-buckling, when: { memberRole: post, height: ">800" },
      expr: Euler屈曲校核（公式待第三部分评审）, onFail: suggest }   # 新增：立柱屈曲
```
```yaml
# rules/connection.yaml —— v0.2（行家评审第二部分修正）
dimensions:
  mobility: [fixed, caster]    # 新维度：脚轮结构载荷特性远劣于固定（水平冲击+扭转+疲劳）
rules:
  - { id: con-001, when: { joint: corner-90, load: light, hiddenRequired: false }, use: corner-bracket-30, confidence: verified }
  - { id: con-002, when: { joint: corner-90, hiddenRequired: true, load: light, loadRole: secondary }, use: internal-30,
      rationale: 仅限外观定位与轻载辅助，禁止主承重（行家）, confidence: verified }
  - { id: con-003, when: { joint: corner-90, load: heavy }, use: anchor-30, confidence: verified }
  - { id: con-004, when: { joint: end-to-face, alignedCoreHole: true }, use: screw-joint-30,
      rationale: 端面攻丝为四类中抗弯最强（轴向预紧）, confidence: verified }
  - { id: con-005, when: { mobility: caster }, action: upgradeConnectionOneLevel + addBraceOrGusset,
      rationale: 推行/转向/越门槛产生水平冲击与扭转，疲劳工况远比静载恶劣（行家）, confidence: verified }

# rules/validation.yaml —— v0.3（行家评审第二部分新增 val-005~007）
rules:
  - { id: val-005, type: bracing,
      when: { anyOf: { height: ">1000", aspectRatio: ">3", "mobility=caster且height": ">800" } },
      action: requireDiagonalBrace,
      rationale: 框架抗侧向刚度靠斜撑/剪力板——晃不是型材不够强，是整体抗剪不足（行家原话，入AI解释话术库）, confidence: verified }
  - { id: val-006, type: forbidden-combo, checks:
        - 内置连接件不得单独承担主承重
        - 长横梁不得只靠单个角码（至少双侧固定，尽量闭合框架）
      confidence: verified }
  - { id: val-007, type: dynamic-load, check: 动态载荷下禁止仅靠型材槽内摩擦连接, confidence: verified }
designPrinciples:
  - 尽量形成闭合框架（行家）
```
```yaml
# —— v0.3 补充（2026-08-04 行家经验叙述：组合连接/主框架惯例/斜撑收紧/脚轮冲击系数）

# ⚠ Schema级修正：接点支持多连接件组合，各带角色（打破"一接点一连接件"假设）
jointModel:
  connectorsPerJoint: multiple
  connectorRoles: [stiffness, positioning]
  industryPattern: 角码(刚度) + 内置(定位) 组合使用（工业设备常见做法）

rules:
  - { id: con-006, type: combo, pattern: { stiffness: corner-bracket-30, positioning: internal-30 },
      rationale: 内置负责定位美观、角码负责刚度——新人误以为内置高级，实际承载不行（行家）, confidence: verified }
  - { id: con-007, when: { frameRole: mainFrame }, prefer: screw-joint-30,
      rationale: 主框架几乎全用端面攻丝——刚度最高/不易松/受力最好，Bosch与item设备大量端攻（行家）, confidence: verified }
  - { id: con-008, when: { joint: beam-bending-corner, scene: [workbench, cnc, laser, aquarium] },
      require: corner-bracket-each-corner, rationale: 横梁受弯四角必加角码（行家）, confidence: verified }

  # val-005 v2：斜撑触发器收紧（行家自述比教材保守）
  - { id: val-005, type: bracing, version: 2,
      when: { anyOf: { height: ">1000", aspectRatio: ">3", mobility: caster, vibration: any, highSpeedMotion: any } },
      action: requireDiagonalBrace, confidence: verified }

  # con-005 v2：脚轮冲击量化 + 升级路径可选
  - { id: con-005, version: 2, when: { mobility: caster },
      impactFactor: 2.5        # 越门槛瞬间冲击2~3倍静载，取中值入设计载荷
      upgradeOptions: [connectionLevel+1, profileSeries+1(如3030→4040), doubleCornerBrackets],
      requireDiagonalBrace: true, confidence: verified }

# 规则引擎判定伪代码 v0.1（用户提炼+补全，未来引擎的验收基准）
# if caster: connection_level+=1; require_brace; design_load=static*2.5
# if height>1000 or aspect>3 or vibration or high_speed: require_brace
# if dynamic_load: min_connection_strength=ANCHOR(4)
# if vibration==HIGH: forbid(corner_bracket_only)
# if member.role==MAIN_BEAM: forbid(internal_connector_only)
# if frame.role==MAIN_FRAME: prefer=END_TAPPING
```
```yaml
# rules/validation.yaml —— v0.4（行家评审第三部分：挠度判据场景化）
deflectionLimits:            # val-002 v2：按场景选限值（行家给出行业分档，全verified）
  diy-furniture: L/275       # 行业 L/250~L/300 取中
  industrial-rack: L/300     # 默认值（行家确认"很好"）
  workbench: L/450           # L/400~L/500
  automation: L/500
  precision: L/800
  optical: L/1000            # 行业 L/1000~L/2000 保守端
rules:
  - { id: val-002, version: 2, type: deflection,
      expr: "5*q*L^4/(384*E*I) <= deflectionLimits[scene]",
      defaultScene: industrial-rack, onFail: suggest, confidence: verified }
```
```yaml
# rules/validation.yaml —— v0.5（行家评审：Euler屈曲完整规范，全verified）
# 认知前提：立柱失效通常不是压碎，而是欧拉屈曲失稳——不能简单用"承重多少kg"判断

  - id: val-004
    version: 2
    type: post-buckling
    formula: "Pcr = π²·E·I / (K·L)²"
    allowable: "P_allow = Pcr / 3"          # 安全系数=3（行家）
    trigger: { postHeight: ">=800" }         # 800mm确认为软件默认触发值（工程经验分界非物理分界）
    K:                                       # 有效长度系数
      default: 1.0                           # 型材框架实际≈0.8~1.0；禁止按两端固定0.5算（过于乐观）
      table: { 两端铰支: 1.0, 悬臂柱: 2.0, 两端固定: 0.5, 一端固定一端铰支: 0.7 }
    slenderness:                             # λ = K·L / r，r = √(I/A)——需截面积A字段！
      "<30": 短柱按材料压缩强度
      "30..80": 过渡区综合判断
      ">80": Euler适用
      ">120": 典型细长柱
    confidence: verified

postHeightBands:                             # 立柱高度分档（行家，verified）
  general: { "<600": 不考虑屈曲, "600..800": 看载荷, "800..1200": 建议校核, ">1200": 必须校核, ">2000": 需加横撑 }
  eu-2020:    { "<600": 安全, "600..800": 仅轻载, ">800": 不推荐单独承重 }
  eu-3030:    { "<1000": 安全, "1000..1500": 需计算, ">1500": 建议升级 }
  eu-4040-s8: { "<1500": 通常安全, "1500..2500": 校核, ">2500": 加横撑 }
```
```yaml
# —— v0.6（2026-08-04 行家评审第四部分：材料接口/装配可行性/公差链，全verified）

# rules/material-interface.yaml v0.2（行家实答替换预设）
rules:
  - id: mat-wood
    material: wood-board
    risk: 吸湿热胀冷缩尺寸变化明显
    mount: [长孔, 压条, 浮动安装]      # 禁止完全锁死
    action: allow_expansion_gap
  - id: mat-glass
    material: glass
    risk: 抗压强/抗拉弱/边缘极脆——风险最大的板材
    forbidden: 玻璃直接压铝槽
    require: [EPDM胶条, 橡胶垫, 尼龙垫片]   # require_soft_interface
  - id: mat-acrylic
    material: acrylic
    thermalExpansion: 70e-6   # /K，大于铝
    example: 1m跨度温差40℃伸长≈2.8mm，锁死→开裂/鼓包
    rule: "length > 500mm → require_clearance"

# rules/assembly.yaml —— 新规则包：装配可行性（"装得上拆不下"四大坑）
rules:
  - { id: asm-001, check: tool_access,          desc: 工具空间校验——装时手能进、拆时扳手进不去 }
  - { id: asm-002, check: assembly_sequence,    desc: 装配顺序校验——先内梁后外框→最后一颗螺丝装不上 }
  - { id: asm-003, check: end_access,           desc: 内置连接件需端面插入口——两端封死即失败, when: { connector: internal } }
  - { id: asm-004, check: modular_design,       desc: 大型框架须拆分设计（运输/维修）, when: { maxDim: ">2000" } }

# rules/tolerance.yaml —— 新规则包：公差链
stack:
  single: ±0.3          # 单根切割公差
  fourFrame: 0.6~1.2    # 四边框累积→对角线误差/门板装不上/玻璃塞不进
  modules: ±0.3×n       # 模块拼接线性累积（3×1000mm→±0.9）
sensitiveScenes: [玻璃嵌入(间隙仅1~2mm), 滑轨平行度, 门框卡滞]
cuttingPrecision:        # 按应用推荐切割精度（行家表）
  普通机架: ±0.5
  工作台: ±0.3
  门框/玻璃框: ±0.2
  精密设备: ±0.1
rule: { id: tol-001, check: "accumulatedError > clearance → warning + 建议提升切割精度档位" }

# 新手三大错误 → 校验映射（行家）
newbieErrors:
  - { error: 只看承重不看刚度, mapsTo: val-002 挠度校验, 话术: 不是断掉而是下垂/摇晃/振动 }
  - { error: 连接件等级不足, mapsTo: 新增val-008, rule: "beam >= 4040 → connection_strength >= medium；主承重节点禁单内置" }
  - { error: 忽略整体稳定性, mapsTo: val-005 斜撑规则(H/W>3), 话术: 型材没问题节点先变形/左右晃 }

# "一看就晃"四模式（AI预警话术素材）
wobblePatterns:
  - 高瘦结构 H/W>3（如400宽×1800高=4.5）
  - 无抗侧向结构（无斜撑/背板/三角）
  - 四角仅普通角码（静载可以，有人扶一下整框摆动）
  - 脚轮+高架（wheel && height>800 → 必须加撑）

# 行家六规则引擎总纲（引擎最小校验集，验收基准）
engineChecklist:
  1: { name: stiffness_check,    condition: span_limit_exceeded,        action: upgrade_profile }
  2: { name: connection_check,   condition: load_high && connector_low, action: warning }
  3: { name: stability_check,    condition: "H/W > 3",                  action: require_brace }
  4: { name: material_interface, condition: glass_or_acrylic,           action: add_soft_layer }
  5: { name: assembly_check,     condition: no_tool_access,             action: redesign }
  6: { name: tolerance_stack,    condition: accumulated_error > clearance, action: warning }
```

## 4. Golden 用例种子

```yaml
tests:
  - { id: t-sel-002-a, rule: sel-002, input: { span: 900, loadKg: 50 }, expect: { use: eu-3030 } }
  - { id: t-sel-004-a, rule: sel-004, input: { span: 500, loadKg: 20, scene: aquarium }, expect: { use: eu-3030 } }  # 2020升级到3030
  - { id: t-con-001-a, rule: con-001, input: { joint: corner-90, load: light }, expect: { use: corner-bracket-30, machiningCount: 0 } }
  - { id: t-con-003-a, rule: con-003, input: { joint: corner-90, load: heavy }, expect: { use: anchor-30, machining: [{ type: through-hole, diameter: 11.5 }] } }
  - { id: t-cut-001, rule: length-derivation, input: { overallWidth: 700, posts: eu-3030, connector: corner-bracket-30 }, expect: { beamLength: 640 } }  # 700-2×30=640,与演示柜实测清单640mm吻合✓
  - { id: t-price-001, rule: pricing, input: { section: eu-3030, length: 123 }, expect: { priceUntaxed: 3.01, tolerance: 0.05 } }  # 与清单页实价核对✓
```

> 注意 t-cut-001：**640mm 是从演示柜真实清单反推验证过的**（总宽700 − 2×立柱30 = 640，与其铝型材清单 L640 完全吻合）——长度推导规则的第一条实证锚点。

## 5. 数据缺口清单

- [ ] eu-4040-s8 的壁厚/米重/单价（下次打开型材库补采）
- [ ] 内置连接件的准确 SKU 与单价
- [ ] 角码的 T型螺母/螺栓实际 SKU 与单价（现为占位）
- [ ] 各截面惯性矩的权威来源（手册/厂商目录，替换现在的近似值）
- [ ] 加工费单价表（清单页只有总额，未见单孔明细价，暂按≈1.6元/孔）- [ ] **各截面的截面积 A**（长细比 r=√(I/A) 计算必需，Schema需新增 mechanics.area 字段——源自Euler规范）

## 6. 评审门禁（同 [[09-知识库Schema设计]] 第9节）

所有标 ⚠ inferred 的条目 + sel-*阈值 + val-002公式 + strengthClass 排序。
