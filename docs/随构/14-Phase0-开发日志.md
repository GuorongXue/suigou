---
tags:
  - 随构
  - Phase0
  - 开发日志
  - 里程碑
created: 2026-08-05
status: Phase 0 收官（M1~M5.1 全过）· Phase 0.5 进行中
---

# 随构 · Phase 0 开发日志

> 仓库：[GuorongXue/suigou](https://github.com/GuorongXue/suigou)（个人账号）· 本地 `c:\Users\wk074\PersonalData\vsCodeProject\suigou`
> 工作纪律：每个改动点单独 git commit（方便回退）；每项进展实时同步本文档。
> 计划来源：准备期收官时定的 solo 五里程碑（见 [[00-随构-项目总览]]）。

## 里程碑看板

| 里程碑 | 内容 | 验收标准 | 状态 |
|---|---|---|---|
| M1 骨架 | Vite+TS+Three.js 查看器 + 知识库 YAML 装载 | 能读知识库画一根型材 | ✅ 2026-08-05 |
| M2 生成引擎 | 参数 → 构件图 → 查看器（纯确定性） | 手写参数生成完整工作台框架+正确下料长度 | ✅ 2026-08-05 |
| M3 规则接入 | 选型/连接决策/校验管道 + BOM/切割清单 | t-cut-001 等 Golden 用例全过；含免责三要素 | ✅ 2026-08-05（Golden 6/6） |
| M4 意图层 | LongCat 抽取（提示词 v0.3）+ 追问策略 | 一句话→方案全链路跑通 | ✅ 2026-08-05（乌龟缸用例两轮验证） |
| M5 回归验收 | 原生组 10 条跑分 | ≥8 条"行家看了没毛病" | ⬜ |

## 开发记录

### 2026-08-05 M1 骨架 ✅（commit 71eed58）

- Vite + React + TS + Three.js 工程落地（`npm run dev`，端口 5173）
- 知识库迁移：[[10-知识库种子数据]] v0.6 终态 → `knowledge/` 真实 YAML
	- `sections/` 3 截面（双层力学参数 range + 显示几何参数 geometry）
	- `connectors/` 4 连接件（strengthClass 行家修正排序：端攻5>锚式4>角码3>内置2）
	- `rules/` 6 规则包（选型/连接/校验含Euler+斜撑五触发/材料接口/装配/公差链）
	- `tests/golden.yaml` Golden 用例（M3 验收基准）
- 查看器：`import.meta.glob` 装载 YAML → 参数化截面挤出 → 切换 2020/3030/4040 全数据驱动
- 浏览器实测通过（截面槽宽/中心孔/米重/价格/惯性矩联动）

### 2026-08-05 M2 生成引擎 ✅（commit 13372de）

- `src/engine/generate.ts` 确定性生成器：FrameSpec（W/D/H/截面/连接件/隔板层数）→ Member[] 构件图（角色/长度/位置/朝向）
- 下料公式与 Golden 同源：梁长 = 总尺寸 − 2×立柱截面 + 2×connector.lengthOffset
- **t-cut-001 实证通过：700宽 + 3030 + 角码 → 横梁 640mm**（与演示柜真实清单吻合）；纵梁 340mm ✓
- 附带产出：切割清单（按长度聚合）、总长/重量/型材估价、连接件兼容性警告、"内置禁单独主承重"警告（val-006 预演）
- 查看器升级为构件图渲染，同截面同长度共享挤出几何（性能路线延续渲染实验结论）
- 默认参数 700×400×720 工作台 16 根构件实时生成

##### 2026-08-05 M2.5 渲染升级 + 连接件可视化 ✅（commit 57c26fc）

用户对比嘉立创效果提出渲染差距，本轮补齐：

- **材质**：RoomEnvironment 环境反射 + PMREM（铝材质感关键）、metalness 0.9 / roughness 0.38、ACES 色调映射
- **光影**：定向光 PCFSoft 阴影（2048 shadow map）+ ShadowMaterial 地面
- **连接件可视化**（引擎新增 Joint 数据：位置/梁轴/朝向/上下侧）：
	- 外露式（角码）→ L 型双板实体，自动贴合梁底面与立柱面，底框朝上其余朝下
	- 隐藏式（锚式/内置/端攻）→ **X-ray 半透明青色螺栓**（depthTest:false 透视可见，嘉立创同款风格）——竞品分析定下的"X-ray 必做项"落地
- 浏览器验证：角码模式 24 个 L 件就位；锚式模式 24 个青色透视件就位

##### 2026-08-05 画布对齐嘉立创 ✅（commit 70e5e25）

对照嘉立创设计器画布逐项对齐：

- **画布观感**：浅蓝灰背景（0xe8edf4）+ 双层网格（细密半透明 + 粗主网格）+ 红/绿轴线 —— 与嘉立创画布同款
- **型材轮廓感**：EdgesGeometry 棱线描边（半透明深灰），槽口暗线清晰
- **交互**（对齐嘉立创操作模式）：左键旋转 / 右键平移 / 滚轮缩放；**点击构件 → 嘉立创蓝高亮 + 右上角详情卡**（角色/截面/下料长度/米重/单根价格）；点空白取消；底部悬浮操作提示条
- 踩坑：删除重建 Viewer.tsx 导致 Vite 模块图缓存损坏白屏（报"App.tsx 无 default 导出"假错误），重启 dev server 解决

**与嘉立创剩余差距（后续按需）**：型材圆角倒角精细截面、连接件厂商级 CAD 模型、拖拽改尺寸（注意：自由编辑在范围红线外，Phase 0 只做"生成→参数微调→重新生成"）、视角立方体、撤销重做。

##### 2026-08-05 查看器精修：尺寸标注 + 参数级微调 ✅（commit a30cac7）

用户反馈三点（阴影干扰/连接件不可选/不能点击显示尺寸和修改），全部落地：

- **去阴影**：关闭 shadowMap 与地面接影，画布更干净
- **连接件可点选**：raycast 覆盖角码板/X-ray 螺栓 → 详情卡（名称/强度等级 x/5/承载角色/安装方式/加工项数/BOM/行家警示 note），选中发蓝色 emissive
- **3D 尺寸标注**：选中构件沿轴显示主线+两端引线+**蓝色 mm 标签**（CSS2DRenderer），嘉立创同款观感
- **参数级微调闭环**：详情卡内直接改下料长度回车 → 引擎反算整体尺寸（横梁→总宽W、纵梁→总深D、立柱→总高H，公式 overall = L + 2×截面 − 2×lengthOffset）→ 重新生成 → 尺寸标签/切割清单/价格全联动。验证：横梁 640→900，W 自动 700→960，清单 900mm×6 ✓
- 选中状态改为 id 寻址（Selection 类型），重新生成后选中保持
- 明确路线：这是"生成→参数微调→重新生成"的红线内交互，不做自由拖拽编辑

##### 2026-08-05 加工特征派生 + 孔位可视化 ✅（commit a49ebfa）

对齐嘉立创"打孔加工"能力（通孔/沉头孔/螺纹孔），但走我们的声明式路线——**孔不是用户手动加的，是连接件声明自动派生的**：

- 引擎新增 `MachiningOp` 派生管道：每个接点按连接件 machining 声明生成孔位（位置/钻孔方向/规格）
	- 锚式 → 梁上距端 **G=19−T+2** 处 Φ11.5 通孔（工艺页公式正式入引擎）
	- 端攻 → 梁端 M8×20 攻丝 + 立柱 Φ9沉Φ14×8.5 沉头孔
	- 内置 → 梁上 Φ8.5 扳手操作孔
	- 角码 → 免加工（0 孔）
- 查看器：孔位橙色半透明圆柱 X-ray 标记（穿透可见，含钻孔方向）
- 侧栏新增**加工清单**（类型/规格/数量），随连接件切换联动：锚式=通孔×24；端攻=攻丝×24+沉头×24
- 与嘉立创的差异化：它要用户自己点"打孔加工"选孔型放置；我们从装配语义自动派生，用户零操作

##### 2026-08-05 渲染修复 + 孔位精细化 ✅（commit e02718c）

用户反馈三点，全部修复：

- **缩放限制**：OrbitControls minDistance 120 / maxDistance 4500，近平面 1→5（改善深度精度），不再能缩到失真
- **型材端面伪影**：根因=截面轮廓四角旋转拼接产生相邻重复顶点 → earcut 三角化退化面片 → 端面对角线伪影。构建 Shape 时去重相邻顶点修复
- **梁端显示异常**：梁端面与立柱侧面精确共面 z-fighting → 渲染层将非立柱构件缩短 0.3mm（纯视觉，不影响下料数据）
- **孔位显示精细化（嘉立创式）**：弃用橙色穿透圆棒 → **表面孔口圆片**：通孔=上下双面深色圆片；沉头孔=立柱外侧面深色孔芯+金属色台阶环（Φ9/Φ14 分层）；扳手孔=可见面单侧圆片；端攻贴柱面被遮挡不出片（仅入加工清单）

##### 2026-08-05 三视图模式 + 出图规划 ✅（commit 7fca0be）

- **三视图模式落地**（用户视角信息分层设计）：顶部工具条切换
	- 外观（默认）：干净框架 + 整体 W/D/H 尺寸线——"这是我要的东西吗"
	- 结构：连接件 + 孔口圆片 + 点选详情——"结实吗怎么连的"
	- 图纸：整体尺寸 + 全部下料长度标注 + 孔位——"我要买什么怎么装"
- 尺寸标注系统泛化为 dims 数组渲染（任意标注线+蓝色标签），选中构件标注在所有模式生效
- **新增 [[15-出图与导出规划]]**：锁定"语义数据为唯一图纸源"架构决策；导出物优先级（切割/BOM CSV → 方案PDF/加工图 → DXF/装配说明 → GLTF/STEP）；细节坑清单（件号系统需按"长度+加工特征"聚合、孔位基准统一端面、免责三要素进标题栏、公差按场景档位自动标）
- ⚠ 记录已知坑：当前 cutList 按长度聚合，同长度不同加工会被错误合并——M3 件号系统修复

##### 2026-08-05 M3 规则引擎主体完成 ✅（commit 7560d30）

行家 v0.6 规则包正式接入生成管道（`src/engine/validate.ts`）：

- **输入维度补齐**：FrameSpec 新增 loadKg / loadType(均布/集中) / scene(挠度限值档) / highRisk(安全系数2.0) / mobility(脚轮冲击2.5)——LLM 抽取 Schema 的同款维度，M4 直接对接
- **可执行规则六条**：
	- val-002 挠度（δ=5qL⁴/384EI 或 PL³/48EI ≤ L/场景档，**限值从 validation.yaml 读取**）
	- val-004 Euler屈曲（H≥800 触发，Pcr/3，K=1.0）
	- val-005 斜撑五触发（>1000/高宽比>3/脚轮）
	- val-001 跨度阈值（sel 行家 verified 表；集中载荷 sel-005 降档×0.75 近似）
	- val-006 内置禁单独主承重（升级为 error）
	- val-008 载荷-连接强度匹配
- **校验上屏**：侧栏分级列表（✖error/⚠warn/ℹinfo/✓pass 含规则ID和行家话术）；**问题构件画布染橙**——嘉立创免责不管的"设计对不对"，我们直接画出来
- **件号系统**：长度+加工特征签名聚合（P1/P2...），修复"同长不同加工被合并"的已记录坑；切割清单含加工摘要列
- **CSV 导出**：切割清单（件号/长度/数量/加工）+ BOM（型材+连接件+配件聚合估价）
- **免责三要素**固定侧栏（估算参考/非专业认证/高风险自确认，07文档措辞）
- 压力验证：2020+1500宽+80kg集中+高风险+脚轮 → 设计载荷×5放大=400kg，6条规则正确触发，挠度263mm vs 允许3.24mm 报错，顶梁染橙 ✓

**M3 剩余**：选型建议引擎（sel 规则正向推荐截面/连接件）+ Golden 用例跑批（t-sel/t-con 需要选型函数）。

##### 2026-08-05 M3 收官：选型引擎 + Golden 6/6 全过 ✅（commit f4c1915）

- **选型引擎**（`src/engine/select.ts`）：sel/con 行家规则正向推荐
	- selectSection：跨度+载荷+载荷类型（sel-005 集中降档）+高风险（sel-004 升级）→ 推荐截面+规则ID+理由
	- selectConnector：接点类型/载荷/隐藏需求/主框架惯例（con-007）/脚轮升档（con-005）→ 推荐连接件
	- UI 侧栏推荐横幅：当前选择 ≠ 推荐时显示"💡 选型建议（sel-002b）：推荐 3030——中跨中载主力系列 [一键应用]"
	- 一致性验证：2020@60kg → 推荐横幅（sel-002b）与 val-002 挠度超限、val-001 跨度超限三方互相印证 ✓
- **Golden 用例跑批**（`src/engine/golden.ts`，读 knowledge/tests/golden.yaml）：侧栏常驻徽章
	- t-sel-002-a ✓ / t-sel-004-a（水族升级）✓ / t-con-001-a（角码0加工）✓ / t-con-003-a（锚式+Φ11.5通孔）✓ / t-cut-001（640mm）✓ / t-price-001 ✓ → **6/6 全部通过**
- **跑批立功**：t-price-001 首跑失败暴露真实数据矛盾——库页截图 23.62/m vs 清单页实价反推 24.47/m（123mm→3.01）。按"实际成交价证据强于页面截图"修正 eu-3030 米价为 24.47，差异已在 YAML 注释记录待复核

> [!success] **M3 里程碑验收达成**：Golden 用例全过 + 校验管道 + 免责三要素齐备。下一步 M4 意图层（LongCat-2.0 抽取 + 追问策略，提示词 v0.3 已在 suigou-lab 验证 95.5%）。

##### 2026-08-05 制造包讨论采纳 + 工艺页全量采集 ✅（commits 726db98, 559769d）

**外部设计讨论（用户提供 随构_设计讨论整理.md）采纳结论**：
- ✅ 采纳：cam-* 规则层（工序链+去毛刺/倒角默认工艺）、攻丝规格补全（M8×1.25 螺距/攻深/单双端）、四类制造文件框架（BOM/Cut List/Process Sheet/Assembly Drawing）、单向公差格式
- ⚠ 修正：示例"距端面40mm"应为 G=19-T+2=19mm（工艺页公式）；攻深 18 → 20（工艺页实标）
- 🎯 战略保持：M4 意图层优先于制造包出图（代差所在）；Schema/数据先补防返工，出图渲染 M5 后

**嘉立创工艺页（xc.jlcfa.com/process）全量采集 → cam.yaml v0.2**：
- **知识库质量三处核对全吻合**：锚式 G=19-T+2/Φ11.5/TPEF-308-0 ✓、M8攻深20 ✓、EDLA-S1-M8-L40 ✓
- 新增：端面攻牙全表（M3~M14 八档）、沉头孔全表（六档）、扳手孔表、锚式通孔表（补40槽10/45系列 G=27-T+2/Φ17）、**口哨连接件盲孔全套**（09文档缺口补齐，含配件BOM）、斜切参数（45°标准/30~150°非标）、推荐螺钉表
- 公差实标：下料分段 L≤1000±0.3 / >1000±0.5；孔径H7级；位置度±0.2；螺纹6H级通止规全检；垂直度≤0.1°；孔口倒角C0.5
- "孔口双面去毛刺"为嘉立创默认工艺（讨论中担心的"不声明直发"在嘉立创不成立，但整料锐边倒钝仍需声明）
- 加工朝向概念（打孔朝前/后，斜切四向）→ Process Sheet 预留字段
- 切割清单 CSV 公差改分段逻辑；螺距 M8×1.25 已补进 screw-joint-30

##### 2026-08-05 壁厚/外形/孔位公差全表采集 ✅（commit c72b3f6）

工艺页锚点区块（#tolerance-section 等三个隐藏 tab）补采完成，cam.yaml → v0.3：

- **壁厚语义澄清**（工艺页图示）：A=翅壁（商城标注值）/ B=封闭腔间壁 / C=隔断壁 / H=非壁厚外形尺寸 / E=孔位基准尺寸
- **具体型号实标**：2020 A=1.4±0.15（**与我们知识库吻合 ✓**）；3030A A=1.5/B=1.3；4040B A=2.4
- **⚠ 发现数据冲突**：知识库 eu-3030 wallThickness=2.0（库页截图）vs 工艺页 3030A A翅壁=1.5——判断为壁厚语义不同（2.0 可能是槽口壁厚 T，锚式 G=19-T+2 依赖它）。冲突已注记在 YAML，**待办：拆分 wallThickness(A)/slotWallThickness(T) 两字段**
- 4040 壁厚缺口回填 2.4（inferred，4040B 型号对应关系待核）；米重/单价仍缺
- 公差三表入库：壁厚公差（区间×A/B/C 双表）、外形尺寸公差（外接圆≤100/100-200 双档）、孔位尺寸公差（H×E 矩阵）——Process Sheet 与出图的公差标注数据源齐了
- 标准依据：GB/T 5237.1-2017 高精级 + GB/T 14846-2014（I类软合金 6063）

##### 2026-08-05 M4 意图层完成：一句话出方案全链路跑通 ✅（commit ab0a4ea）

- **抽取模块**（`src/engine/extract.ts`）：提示词 v0.3 内嵌（与 suigou-lab 同源，95.5% 验证），LongCat-2.0，三坑全防（reasoning_content 兜底 / response_format 400 降级 / ```` ``` ````围栏剥离）；Key 存 localStorage 不进代码库
- **CORS 方案**：vite dev proxy `/api/longcat` → api.longcat.chat；踩坑：公司网络证书链 → `secure: false`（生产需后端转发，Phase 1 事项）
- **意图映射**（`src/engine/intent.ts`）：抽取 JSON → FrameSpec；缺失字段保守默认+假设记录（宽800/深400/高750/载荷30kg）；场景映射（aquarium→机架挠度档+highRisk）；**选型引擎自动定截面和连接件**（选型依据写进假设列表）
- **追问策略**：安全优先（高风险强制问载荷）→ mobility unknown 问脚轮 → _missing 前2条，最多3问；多轮=历史+补充合并重抽（实验验证的单轮路径）
- **UI**：侧栏顶部输入框（无Key时引导配置）；风险提示红条 / 追问蓝条 / AI假设与选型依据折叠列表；抽取结果直接回填全部参数控件
- **乌龟缸用例全链路实测**：
	- 第1轮："乌龟缸架子 缸60厘米 40公斤 没工具 结实点" → W600/40kg/集中/水族→highRisk 自动勾选→安全系数2.0→**选型自动升到4040**（sel-004）→风险提示"缸体破裂/潮湿防锈"→追问脚轮/深度/高度
	- 第2轮："不用轮子 高70 深45" → D450/H700 回填，脚轮问题消失，追问收敛到仅剩宽度确认 ✓
- req-001 悬挑灯的"部分支持降级"体验未做（Phase 0 生成器只支持正交框架，降级提示是 M5 后打磨项）

> [!success] **M4 里程碑验收达成**：一句话→方案全链路跑通。Phase 0 进度 4/5，只剩 M5 回归验收（原生组 10 条跑分 ≥8 过）。

##### 2026-08-05 M4.5 用户实测反馈修复 ✅（commit 5511ba6）

用户实测提出四个问题 + 设计反思（"怎样才是好设计"），全部落地：

**设计病根诊断：对话和滑杆是"两个世界"**——AI 单向写参数、手动调整 AI 毫不知情。修复原则：
1. **单一事实源**：滑杆改动记账（🔒锁定提示），下一轮注入模型上下文 `[当前方案状态…][用户已手动调整且必须保持…]`，AI 不覆盖手调值（除非用户明说改口）
2. **像问诊不像填表**：追问一次只出一个（安全>成本>外观），答完自然出下一个
3. **信任来自可追溯**：对话历史气泡流（用户消息/AI 理解摘要），AI 每轮回复"已更新方案：宽×深×高+截面+载荷"
4. **诚实边界**：门板/把手/灯具等超纲需求 → "🚧已记录但当前版本暂不支持（不会丢失）"清单，绝不静默丢弃

**板材系统落地**（最严重问题：panels 抽取字段被吞）：
- FrameSpec 新增 topPanel/shelfPanel（木/玻璃/亚克力/洞洞板）；生成器产出 PanelItem（尺寸=框内净空−材质间隙，安装方式挂 mat-* 规则话术）
- 查看器真渲染：木色实体/玻璃半透明/亚克力磨砂/洞洞板
- mat-* 校验上屏：玻璃必须钢化+EPDM胶条（warn）、亚克力>500 热胀间隙（info）、木板浮动安装（info）
- UI 加顶面板/隔板材质选择器（手动路径同权）

**茶水柜用例验证**："茶水柜 顶面木板 中间玻璃隔板 亚克力门带把手 宽80高90深40" → 尺寸cm→mm正确、木顶板+玻璃隔板渲染、mat-glass/mat-wood 警告、**门板（亚克力）进降级清单**、追问只问载荷一条 ✓

##### 2026-08-05 评测采纳与 P0 修复计划（源：[[16-产品体验与结构能力评测]]）

**评测结论全盘接受**。核心判断认同："当前偏重生成几何外形，尚未建立装配关系和侧向稳定性"——画出来 ≠ 连接了。

**修复排期**（按 9.6 建议顺序）：

| # | 项 | 规模 | 状态 |
|---|---|---|---|
| 1 | 导出质量闸门（invalid 禁导出，valid/needs-confirmation/invalid/incomplete 四态） | 小 | 🔧 本轮 |
| 2 | 兼容组合解析器（截面+连接件原子校验，不兼容=error；下拉按截面过滤） | 小 | 🔧 本轮 |
| 3 | 件号签名重构（加工面/方向/端部进签名，防镜像件合并） | 小 | 🔧 本轮 |
| 4 | 装配数据模型 Joint/Mount/Fastener/Accessory（消除字符串式"已安装"） | 大 | ⬜ 下一轮专项 |
| 5 | 板材真实固定（top-overlay/in-frame 两模式+固定点+紧固件BOM）修复悬空 bug | 中 | ⬜ 随#4 |
| 6 | 脚轮/板材进 BOM 与重量价格 | 中 | ⬜ 随#4 |
| 7 | 侧向稳定性"未验证"状态（不再只凭竖向挠度显示通过） | 小 | ⬜ 随#4 |
| 8 | 手动锁定改 explicitlyMentionedFields（弃数字正则）；追问成对传递；"不会丢失"改口径 | 中 | ⬜ P1 |
| 9 | UI 三栏工作台重构 + 研发语言（Golden/val-xxx）移出主界面 | 中 | ⬜ P1 |

##### 2026-08-05 P0 可信度三连修 ✅（commit df174e3，对应 16号评测 9.2.1/9.2.2/9.2.5）

- **兼容组合原子校验**（9.2.1）：series+slotWidth 双条件；不兼容 → `compat-001` error（原来只是 warning 文字）；连接件下拉按当前截面过滤，不兼容项禁选+标注
- **导出质量闸门**（9.2.2）：FrameModel 新增四态 `status`（valid / needs-confirmation / invalid / incomplete）；error→invalid 禁止导出（按钮禁用 + **导出函数层自校验**双保险）；warn→needs-confirmation 需确认弹窗；校验标题挂状态徽章
- **件号签名重构**（9.2.5）：签名从"长度+加工类型"升级为"长度+类型规格@距起端距离/加工面方向"——上下表面开孔不同的镜像梁不再共用件号
- 验证：2020+角码30 → compat-001 报错+状态"存在错误禁止导出"+按钮禁用+下拉全部标注不兼容 ✓；切回 3030 → "可导出"恢复，Golden 6/6 保持 ✓
- 剩余 P0（装配数据模型 Joint/Mount/Fastener、板材悬空修复、脚轮/板材进BOM、侧向稳定性状态）为下一轮专项——涉及数据模型架构，不适合与本轮混做

##### 2026-08-05 装配层专项完成 ✅（commit 8291dfe，对应 16号评测第四章 + 9.2.3/9.2.4）

评测核心论断"画出来 ≠ 连接了"的架构级回应：

- **装配数据模型**：新增 `MountItem`（板材/附件与主体的固定关系：方式/紧固件/固定点坐标）+ `AccessoryItem`（脚轮等附件），消除"字符串式已安装"
- **板材悬空 bug 修复**（9.2.3）：废弃"框内缩进"单一公式 → 两种真实模式：
	- `top-overlay` 顶板覆盖整框（W×D 齐平，底面落顶梁上表面）
	- `shelf-overlap` 隔板四边各搭 15mm 在梁上表面（真实支撑接触）
	- 每块板生成 Mount：木/洞洞板=T型螺母+螺钉×4；玻璃/亚克力=EPDM胶垫+压条×4；固定点坐标落库
- **脚轮最小闭环**（9.2.4）：mobility=caster → 4个丝杆脚轮 AccessoryItem（渲染轮体+叉架）+ **柱底端面 M8×20 攻牙自动进加工清单**（装配关系派生加工特征的又一实例）+ Mount 记固定关系
- **BOM 完整性**：板材（材质×尺寸）、装配层紧固件聚合（T型螺母/螺钉/胶垫/压条）、脚轮全部进 BOM；**重量含板材面密度+脚轮**（验证：7.4kg→16.9kg）
- **val-lateral 侧向稳定性状态**：无斜撑体系是事实 → 高架 warn"未验证≠不会晃"/矮架 info，杜绝"全绿=安全"假象
- 结构视图新增蓝色固定点小球（Mount points 可视化）
- 验证截图：木顶板+玻璃隔板+四脚轮完整移动工作台，con-005/val-005/val-lateral 联动正确

##### 2026-08-05 P1 意图层三问题修复 ✅（commit 441bbb9，对应 16号评测 9.4）

- **9.4.1 锁定误判**：弃用"本轮文本含数字"正则 → 抽取 Schema 新增 `_explicitFields`（模型判定本轮明确提到/修改的字段路径，规则11）；锁定字段仅当其路径出现在 _explicitFields 时才解锁并采用 AI 新值，与"改口请明说"文案一致
- **9.4.2 追问不成对**：extractIntent 的标准 history 真正启用——最近6条对话按 assistant/user 轮次成对传递（AI 追问在上下文里，"要/不要/50公斤"指代明确）；当前 FrameSpec 以结构化 JSON 注入而非自然语言备注
- **9.4.3 "不会丢失"失实**：草稿 localStorage 持久化（spec/chat/锁定项/暂不支持清单），刷新恢复；文案改"已存入方案草稿"；标题栏加"新方案"按钮清空重来
- 顺手（评测 2.2）：W/D/H 尺寸加数字输入框（滑杆只作快速微调）；Golden/知识库统计等研发语言收进"开发者诊断"折叠，不再暴露给普通用户

**16号评测消化进度**：P0 全清 ✅，9.4 意图层全清 ✅。剩余：UI 三栏工作台重构（2.3）、结构能力 P1（斜撑/侧板/矩形梁 SKU）、方案解释/多方案对比（P2）。

##### 2026-08-05 UI 三栏工作台重构 ✅（commit 11c88f2，对应 16号评测 2.3/2.4）

按评测推荐布局落地：

- **顶栏**：品牌 + 方案状态徽章（✓可制造 / ⚠N项警告 / ✖N项错误）+ 构件数/重量/价格摘要 + 新方案 + 全局导出按钮——生成完成后第一眼看到"可制造性状态、风险数量、重量、参考价"（评测 2.4.4）
- **左栏 340px**（用户想要什么）：对话 + 参数控件 + 选型建议
- **中间画布**（方案长什么样）：三视图模式 + 点选详情
- **右栏 340px**（为什么可靠、要买什么）：结构校验 + 切割清单 + 加工清单 + 免责三要素 + 开发者诊断折叠
- 校验、清单、导出不再需要长距离滚动（评测 2.2.3 修复）

**16号评测消化状态**：界面部分（2.2/2.3/2.4）主体完成；剩余为结构能力 P1（斜撑/侧板/矩形梁）与 P2（多方案对比/门板/爆炸图）——量级大，留作后续专项。

##### 2026-08-05 P1 结构能力：侧板 + 斜撑 ✅（commit 8a8bb31，对应 16号评测 P1）

- **暂不支持清单去重**（用户报告的重复项 bug）：Set 计数合并，"抽屉（板材）×2"而非重复两行
- **背/左/右侧板**：外贴式（贴框架外侧面），Mount 四角固定于立柱，材质走 mat-* 规则；**兼作抗剪体系**——这是评测"侧板能力"与 val-lateral 的逻辑闭环
- **背面对角斜撑**：真实型材构件（Member role=brace，同截面），长度=对角线，**两端斜切角度自动计算并提示**（嘉立创非标斜切 30~150° 适用性检查）；进切割清单独立件号
- **校验联动**：配置斜撑/侧板后 val-005 和 val-lateral 从 warn 转 **pass**（"已配置背面斜撑+侧围板抗剪"）——用户第一次能看到"问题→动作→变绿"的完整闭环
- 抽取映射：panels position='side' → 背板（不再降级）；UI 加背/左/右三下拉 + 斜撑勾选
- 验证：1500高+脚轮+洞洞板背板+斜撑 → 三触发全命中且全部 pass，斜撑 P4 1720mm 斜切56.9° 进清单，渲染正确
- 遗留：2040 矩形梁 SKU 需 buildSectionShape 支持矩形截面 + 梁柱双截面架构，量级中等，单独排期；_missing 英文字段名偶尔暴露给用户（"请确认：panels.material"）待修

---

#### 2026-08-05 日终总结

**一天完成 Phase 0 里程碑 M1→M4 + 两轮评测消化**，commit 链：71eed58(M1) → 13372de(M2) → 57c26fc/70e5e25/a30cac7/a49ebfa/e02718c/7fca0be(查看器五轮打磨) → 7560d30/f4c1915(M3+Golden 6/6) → 726db98/559769d/c72b3f6(制造包+工艺页采集) → ab0a4ea(M4) → 5511ba6(M4.5用户反馈) → df174e3/8291dfe/441bbb9/11c88f2/8a8bb31(16号评测 P0/装配层/意图层/三栏UI/侧板斜撑)。

**当前产品能力快照**：
- 一句话→抽取(LongCat-2.0 95.5%)→追问(一次一问/成对上下文)→确定性生成→校验(挠度/屈曲/斜撑/跨度/兼容/侧向)→三栏工作台展示→CSV导出（质量闸门）
- 结构：正交框架+顶板/隔板/背左右侧板+背面斜撑+脚轮；装配层 Mount/Fastener/Accessory；件号系统含加工面签名
- 知识库：3截面+4连接件+8规则包（v0.6+cam v0.3 工艺页全量公差）；Golden 6/6
- 交互：草稿持久化、手动锁定(_explicitFields语义解锁)、诚实降级清单、免责三要素

**明日/下次待办**（优先级序）：
1. **M5 回归验收**：13号测试集原生组 10 条逐条跑分，≥8 条"行家看了没毛病" → Phase 0 收官
2. 2040 矩形梁：buildSectionShape 矩形支持 + 梁柱双截面架构
3. 小修：_missing 英文字段名暴露（"panels.material"）转中文
4. 16号评测 P2：多方案对比/门板铰链/爆炸图（Phase 1 级）
5. 生产部署时 LongCat 代理需后端转发（现为 vite dev proxy + secure:false）

##### 2026-08-06 斜撑装配化修复 ✅（commit 4b95317，用户实测反馈）

用户指出斜撑两个真实问题：①没有与主体的连接件和孔道 ②有隔板层时直接穿过横梁（穿模），修复：

- **按层分段（之字形）**：斜撑在每个隔板横梁处断开，每格一段、方向交替——工程实践的"拼接连接"处理；层间净空<100mm 的格子自动跳过
- **端部装配化**：每段两端 Φ7 固定孔（穿 M6 螺栓 → 柱/梁槽内 T 型螺母压接）→ 进加工清单（"Φ7(斜撑端)×2"挂在件号上）；每段一条 Mount（fasteners: t-nut-m6×2 + bolt-m6-l16×2）→ 进 BOM；固定点结构视图可见
- **斜切角语义修正**：报给加工的是端面切割角（90°−杆件倾角），如倾角 25.8° → 切割角 64.2°，并按嘉立创非标斜切 30~150° 范围提示
- MountItem.targetType 扩展支持 'member'
- 验证：1500高+2层隔板 → 斜撑分 3 段（P4×2/P5×1 独立件号），Φ7 孔×6 进加工清单，渲染无穿模 ✓

##### 2026-08-06 视角立方体 ✅（commit c9157d1，用户对标嘉立创请求）

- 右上角 **ViewCube**（Three.js 手写，无新依赖）：独立小场景渲染，六面中文标签（上/下/左/右/前/后，CanvasTexture）
- **姿态同步**：cube 四元数 = 主相机四元数的逆，每帧同步——转主画布立方体跟着转
- **点面切视角**：raycast 命中面 → 局部法线即主场景世界方向（姿态互逆的数学红利）→ 保持当前距离切到正交视角，smoothstep 补间动画；俯/仰视加微偏移防 lookAt 退化
- 构件/连接件详情卡下移 130px 避让立方体
- 至此嘉立创画布的标配交互（网格/轴线/三键操作/点选/尺寸标注/视角立方体）全部对齐

##### 2026-08-06 · 图纸模式专业化（commit 4340542）

**用户反馈**："外观就不要显示HWD了，图纸部分站在图纸专家角度完善，现在太差劲了。"

**改动内容**：
- **外观模式**：去掉 W/D/H 尺寸标注，还原干净的渲染形态（选中构件仍显示尺寸）
- **图纸模式重构**（对标工程图纸规范）：
	- 白底线框风：白色背景 + 浅白体色 + 深色棱线，隐藏地面网格/坐标轴
	- 伪正交投影：fov 50→15 + tan 比例距离补偿，消除透视变形（图纸禁忌），并多留 25% 边距容纳标注
	- 标准视图按钮：主视图 / 俯视图 / 左视图 / 轴测，smoothstep 补间切换
	- 完整尺寸链：总尺寸 W/D/H（外偏移 110）+ 左前柱逐段层间距（如 485/500/485）+ 每种下料长度代表标注
	- 件号球标：每个件号（P1~Pn）在第一根构件中点挂圆形球标，与右侧"切割清单（按件号）"直接对照——图与清单互查是图纸核心用途
- 技术点：材质随 drawing 状态切换（`mesh.userData.baseColor` 保存原色供高亮恢复）；fov/距离切换用 prevDrawing ref 保证只在模式变化时执行一次

**验证**：tsc 通过；Playwright 实测主视图正交效果、尺寸链、球标、切回外观无标注均正常。

##### 2026-08-06 · P0 可用性缺口修复（commit e59f9b4）

**上游**：[[19-四视角可用性差距评审]]（用户四视角实测反馈，M5 前置）。

**改动**：
- **价格知识库三件套**（架构 8.1 补目录）：`knowledge/panels.yaml`（板材厚度/面密度/每平米单价/固定方式/孔径）、`knowledge/fasteners.yaml`（T型螺母/螺栓/胶垫/压条/脚轮等单价，端攻螺栓为配件页实价 verified，其余 inferred 估价）、`knowledge/rules/pricing.yaml`（打孔/攻牙/沉头/斜切/板孔按处计价）
- **面板固定孔进模型**：t-nut-screw 材质（木板/洞洞板）四角 Φ7 孔（顶板距边 s/2、隔板搭接区中心、侧背板孔心落柱中心线）；玻璃/亚克力胶垫压条免钻孔——修复"照清单做出来的板装不上"的制造断链
- **板材下料清单**：同材质+尺寸+孔位聚合为件号 B1/B2…，含单件估价（面积×单价+钻孔费），进 FrameModel.panelList
- **全 BOM 价格明细**：totals.cost = 型材+板材+连接件+紧固件+型材加工费+附件 → 合计；脚轮归附件项防重计
- **UI**：顶栏"合计约 ¥N"；右栏新增板材清单表+价格明细表（标注"非实价报价"）；导出 CSV 板材行填价填孔、BOM 加合计行
- generate.ts 的 PANEL_SPEC 硬编码下沉到 knowledge/panels.yaml（知识库驱动）

**实测**：1000×400×1500 三层洞洞板+玻璃隔板+脚轮方案 → 合计 ¥865.72（型材474/板材208/连接件58/紧固件20/加工费74/附件32），板材清单 B1~B3 孔位注齐全。

##### 2026-08-06 · P1 交互与单件加工图（commits 7a87981, 4e9f5e0）

**上游**：[[19-四视角可用性差距评审]] P1 两项。

**改动**：
- **侧栏折叠**（7a87981）：左右 340px 岛台各配窄把手按钮（‹/›），折叠后 3D 区全宽——修复用户"岛台不能收缩"反馈
- **Mount 固定点语义化**（7a87981）：结构模式小蓝球加编号标签 M1..Mn，悬停显示"固定方式｜紧固件×数量｜说明"（如 M5: T型螺母+螺栓｜t-nut-m6×2｜斜撑段端部压接）——修复"看不到斜撑/面板连接点"反馈
- **单件加工图**（4e9f5e0，工人视角"最后一公里"）：
	- 引擎：CutListItem 增结构化 ops（类型/规格/距起端 mm/加工面/孔径），与件号签名同源
	- 新组件 [PartDrawing.tsx](src/components/PartDrawing.tsx)：SVG 出图——按加工面分条带视图（"打孔面：上表面(y1)"）、左端基准符号、孔位尺寸链、孔规格 Φ11.5、总长+公差 940(±0.3)、端面加工注记（攻牙在哪端）、去毛刺注
	- 交互：右栏切割清单件号变链接，点击弹出加工图浮层
- **验证**：P2 加工图实测（940mm 梁、两端 Φ11.5 锚式孔 @19/921）；M1~M11 标签+悬停全部渲染；折叠往返正常。

##### 2026-08-06 · M5 回归验收完成（commits 99a798c, f7cfbc2）—— Phase 0 收官 ✅

**CLI 跑批**（99a798c，补收官条件"命令一次执行"）：`npm run golden` = 知识库完整性检查（必需字段/悬空引用/SKU价格覆盖/加工计价）+ Golden 用例 + 生成冒烟含架构 13.2 强制不变量。顺带抓出 4040 无米价（补 inferred 估值 42元/m、1.5kg/m 待实采）。

**M5 跑批器**（f7cfbc2）：`npm run m5` 用 LongCat-2.0 真实 API 跑原生组 req-001~010 原话，输出完整记录链 out/m5-report.{json,md}；extract.ts 增加 Node 环境兼容（直连+环境变量 Key）。

**结果**：10/10 达 80 分（均值 94），全部收官硬条件满足，详见 [[20-M5回归验收报告]]。

**M5 现场抓出并修复的两个真缺陷**：
1. con-002 规则冲突：隐藏式偏好推荐内置件，但内置件禁单独主承重（val-006）——推荐引擎自产 invalid。修正为推锚式 + Golden t-con-002-a 回归用例
2. 选型联动断裂：截面升 4040 后 anchor-30 不适配（compat-001 error）。intent 层加兼容回退：改用该截面兼容的最强主承重件并记假设

**M5.1 遗留**（提示词/知识增量，不动架构）：振动设备示例、水电组合风险、预算抽取、材料白名单外记录、40 系列连接件补录（M7）。

**另修**：脚轮穿地（b09d01c，地面网格下移到轮底平面）。

##### 2026-08-06 · M5.1 回归修正完成（commit 2efd6e9）

**处置 [[20-M5回归验收报告]] 缺陷 #3~#6**（提示词 v0.3→v0.4 + 引擎两处）：
- **振动全链路补齐**（缺陷#3 + 隐藏缺陷）：提示词加高速运动设备示例（拓竹/Voron/CNC）；发现 FrameSpec 根本没有 vibration 字段——val-005 声明"五触发"实际只实现三触发。补 `spec.vibration`（抽取双源：environment.vibration 或 LLM 归入 _riskFlags 均接住）→ req-002 现在正确触发"设备振动工况"斜撑建议
- **水电组合风险**（#4）：提示词规则 13 → req-003 riskFlags 出 water-electric ✓
- **预算抽取**（#5a）：提示词规则 14 + intent 透传"安全优先于预算"假设
- **超高档横撑**（#5b）：val-postband 规则（>2000mm 建议中部横撑，源 postHeightBands 行家档位）→ req-009 触发 ✓
- **材料白名单**（#6）：提示词规则 15 → 海洋板→other+记录原词 ✓
- **额外抓出的几何错误**：req-009 高 2400 被 intent clamp 静默截成 2000（M5 评分时漏看）。clamp 上限放宽到 3000（屈曲校验管安全），超限时显式记假设；UI 滑杆同步 3000 + 振动复选框

**验证**：M5 全集重跑 10/10 无 invalid 无异常，四项增量全部生效；`npm run golden` 7/7 无回归。M5.1 验收条件（全集重跑无新回归 + 缺陷有测试）达成。剩余：40 系列连接件补录归入 M7。

##### 2026-08-06 · Phase 0.5 第一批完成（commits 491a486, 980ff8c, 637fdda, ff8d4ed）

**上游**：[[19-四视角可用性差距评审]] P2 路线第一批（套裁/底板/LED/围网；把手随门板归第二批——无门无载体）。

- **套裁下料优化**（491a486）：nesting.ts FFD 首次适应递减 + 锯口 5mm（cam.yaml nesting 参数，原料 6000mm）；右栏"下料方案"块显示原料根数/利用率/每根切法/余料。实测 1500×900 桌架：4 根原料利用率 77%，余料明细到毫米——采购可直接照单买料
- **底板**（980ff8c）：FrameSpec.bottomPanel 全链路，复用隔板搭梁逻辑（Mount/孔位/清单/价格自动继承）；意图层 position bottom + 提示词枚举扩展
- **LED 灯条**（637fdda）：顶框前梁下槽内嵌（mat-004 规则），AccessoryItem 扩 kind/lengthMm，Mount method slot-embed；计价=灯条按米+电源；渲染自发光长条；顺带修 BOM 聚合中脚轮本体重复计行的旧问题
- **围网**（ff8d4ed）：wire-mesh 材质全链路（2kg/m²、40元/m²、U型包边四角螺栓）；**关键工程语义**：val-005/val-lateral 修正——围网是围护件非剪力板，不计入抗剪体系（16号评测"区分围护板与结构剪力板"的落地）

**验证**：tsc + `npm run golden` 每步全过；浏览器实测底板/围网/LED/套裁同屏正常。
**剩余 P2**：第二批=门板(槽装合页)+把手+装配说明书；第三批=抽屉(动拓扑,与2040矩形梁一起规划)。

##### 2026-08-06 · Phase 0.5 第二批完成（commits 298b544, 63354d4）

- **门板+把手**（298b544）：正面单开门全链路——门尺寸=开口−每边3mm间隙；硬质门（木/洞洞板）=槽装合页×2入左柱前槽+磁吸扣+拉手96（门板 2×Φ5 把手孔进加工清单），软质门（玻璃/亚克力）=玻璃门铰夹式+粘贴把手免钻孔；门宽>600 警告铰链下垂建议双开；Mount method `hinge`；渲染门板+金属把手圆柱；**意图层 position door 从"降级"转为"支持"**（M5 中 req-008/009 的门需求现在能真做）；fasteners.yaml 增 5 个门五金 SKU
- **装配说明书**（63354d4）：assembly.ts 纯派生（不膨胀 FrameModel）——从 Joint 按接点高度分层编排（底框立柱→逐层横梁→顶框校方"对角线差≤2mm"）、Mount 按 mode 分步（斜撑/水平板/围护/门/脚轮/LED）、收尾终检；每步=件号×数量（按构件去重，修复接点重复计数 bug）+紧固件人读名×数量+工具推导（M6→5mm 内六角）+工艺注记；UI 右栏折叠展示 + 顶栏"导出装配说明"txt（过质量闸门）。实测 9 步说明书与 BOM/加工图三件套闭环——工人拿"切割清单+单件加工图+装配说明"可以完整施工

**Phase 0.5 剩余**：第三批抽屉（动生成器拓扑：层内滑轨梁对+成品抽屉盒 SKU，与 2040 矩形梁架构一起规划）。

##### 2026-08-06 · 图纸模式缩放失真修复 + 全文档对账（commit 91d6382）

- **用户实测反馈**：图纸模式放大缩小时模型出现斜切缺角（"失真"）。根因=窄 fov 距离补偿把相机拉到 10000+ 而 far 裁剪面只有 10000，远/近平面切进几何。修复：far 10000→40000；图纸模式缩放区间 800~30000 与外观模式 120~4500 分开
- **全文档对账**：00 总览（状态/索引/能力快照）、04 准备清单（归档+A区未勾项转 Phase 1）、16 评测（已消化归档）、17 架构（baseline 91d6382+完成判据进展注记）、18 实施计划（S0/M5/M5.1 验收结果+近期下一步重写：建议下一主线=真实用户验证 vs 抽屉架构改造二选一）、19 评审（P2 前两批完成状态）全部更新至当前基线

## 遗留/注意事项

- 4040 数据缺口未补：壁厚/米重/单价（下次打开嘉立创型材库顺手抄）
- 编辑器 TS 偶报 yaml/import.meta.glob 假错误（语言服务缓存），以命令行 `npx tsc --noEmit` 为准
- `archive/` 含旧会话聊天导出（内含 API key），已 gitignore，绝不能提交
- Git 凭据：个人账号走 remote URL 用户名前缀 + PAT（credential.useHttpPath 已开），与公司账号隔离


##### 2026-08-06 电脑桌人体工学语义修正（下层更大 + 上层更浅）✅（commit 598da57）

- **用户反馈问题**：电脑桌两层虽然能分层，但默认“上下空间等分、上下等深”不符合真实使用；下层需要更大腿部/操作空间，上层置物应明显更浅。
- **实现策略**：
  - 在 `FrameSpec` 新增工作台语义参数：`workbenchLowerZoneRatio`、`workbenchUpperShelfDepthRatio`。
  - `intentToSpec` 在 `scene=workbench` 时注入默认人体工学假设（下层净空优先、上层浅搁板）。
  - `generateFrame` 改为工作台专用非均匀层高：首层按下层占比分配；上层逐级压缩间距。
  - 隔板深度改为可变：越上层越浅，默认上层约 58% 深度，并后靠布置（保留前侧操作空间）。
  - UI 新增“电脑桌语义（人体工学）”两个滑杆，支持直接调下层净空占比、上层浅搁板深度占比。
- **验证结果**：
  - `npx tsc -p tsconfig.json --noEmit`：通过。
  - `npm run golden`：通过。
  - `npm run build`：通过。
  - `npm run m5`：通过（脚本键名应使用 `LONGCAT_API_KEY`，非 `LLM_API_KEY`）。
- **效果结论**：工作台默认行为从“结构均分”升级为“使用语义优先”，更贴近真实电脑桌（下层坐姿空间充分，上层展示/收纳层更浅）。


##### 2026-08-07 电脑桌形态约束落地（避免“电脑桌长成高柜”）✅（commit a388236）

- **问题复现**：用户反馈当前图形虽有“下大上浅”分层，但整体形态仍像高柜，与电脑桌语义不符（超高、封闭门板/全高侧背板）。
- **本次修正**：
  - `intentToSpec`：`scene=workbench` 时将高度约束到 680~900mm 语义区间；若超出则写入显式假设说明。
  - `intentToSpec`：工作台默认开放式，AI 抽取阶段不再生成门板/全高侧背板（避免柜体化）。
  - `App`：状态归一化加入工作台约束，切换/加载工作台方案时自动应用桌面高度区间和开放式拓扑默认。
  - `validateFrame`：新增 `val-workbench-height` 与 `val-workbench-topology` 提示规则，明确提醒“工作台语义偏离”。
- **页面实测**（localhost）：
  - 原草稿 `H=2640` 在工作台场景自动归一到 `H=900`；
  - 背板/左侧板/右侧板/门板自动归零为“无”；
  - 人体工学参数保持 `下层65% / 上层55%`。
- **回归结果**：
  - `npx tsc -p tsconfig.json --noEmit`：通过；
  - `npm run golden`：通过；
  - `npm run build`：通过。


##### 2026-08-07 电脑桌可用性修正（可坐入、可操作）✅（commit 66c2901）

- **用户痛点**："为什么还有底板，人的头/身体怎么进去"——此前把工作台语义误建成类似立柜/货架，实用性不成立。
- **核心修正**：
  - 工作台从“下层占比驱动”改为“主桌面绝对高度驱动”：新增 `workbenchDeskTopHeightMm`（默认 740mm，建议 680~800）。
  - 工作台总高语义改为“双层桌架总高”区间 1100~1800（不再错误等同桌面高度）。
  - 工作台强制开放式：`bottomPanel/doorPanel/backPanel/leftPanel/rightPanel` 默认关闭，避免封堵腿部空间和柜体化。
  - 工作台至少保留 1 层隔板作为主桌面；若未指定隔板材质，自动补木板作为桌面板。
  - 生成层位改为：第一层锚定主桌面高度；上层再按浅层后靠策略生成（上层深度比仍可调）。
- **校验增强**：新增主桌面高度可用性提示（`val-workbench-desk-top`），防止高度跑偏影响坐姿操作。
- **实测结果（localhost）**：
  - 总高由旧草稿 2640 自动归一为 1100；
  - 主桌面高度显示 740mm；
  - 底板/门板/全高侧背板自动清空；
  - 形态从“柜体”回到“可坐入电脑桌”。
- **回归结果**：`tsc` 通过，`npm run build` 通过，`npm run golden` 全通过。


##### 2026-08-07 真实物件档位知识库（archetypes）落地 ✅（commit 363f997）

- **用户要求**：“先去网上学一学每种物件的真实样子，再开发”——不能让引擎凭空想象家具形态。
- **资料采集**（公开来源，百度百科+聚合搜索 2026-08）：
  - **电脑桌**：桌面高 72~76cm（标准 75）；深度 60cm 舒适线（双屏 65~70，<55 压迫视线）；宽度单人 100~120cm、双屏 140~160cm+；上层置物为辅层（浅、靠后）。
  - **家用置物架**：深度 25~45cm 按物品定；层高约 30cm（重型 35~50 可调）；总高 150~180cm；>1.6m 建议上墙防倾倒。
  - **衣柜**：深度 55~60cm（<55 顶门、>60 死角）；短衣区净高 90~100cm，长衣区 ≥130（推荐 140~150）；总高常 240cm。
  - **鞋柜**：净深 35cm（含鞋盒 ≥38）；层高分区 12~20cm（靴区 25~50）；底部留空 15~20cm。
  - **鱼缸底柜**：高 70~75cm（海水 80~90）；总高含缸 1.2~1.4m；柜面比缸底大 3~5cm；承重 ≥ 水重×1.5（≥200kg）；大缸必须金属框架+全支撑面。
- **工程落地**：
  - 新建 `knowledge/archetypes.yaml`：以上档位全部数据化（min/std/max + 拓扑特征 + 出处注记）。
  - `KnowledgeBase` 增加 `archetypes` 域；浏览器 loader 与 Node loader（kb-node）双侧接入。
  - `intent.ts`/`validate.ts` 的工作台档位改为从 archetypes 读取（去掉硬编码），新增 `val-workbench-depth`（桌面深度 <550 警告，来源=显示器距离伤眼/键盘无处安放）。
- **回归**：`tsc`/`golden`/`build` 全通过。
- **后续**：archetypes 待行家评审（confidence=public）；下一步可将 storage-rack/wardrobe/shoe-rack/aquarium-stand 档位接入对应场景的默认值与校验。


##### 2026-08-07 电脑桌语义链路诊断修复（“完全不能用”→形态正确）✅（commit 2a6b69d）

- **用户复测原话**：req-004（1.5m×90cm 电脑桌+洞洞板+上层展览架）生成 750 高矮框+洞洞板当隔板——完全不能用。
- **诊断方法**：新增 `scripts/diag.ts` 单句诊断（抽取→意图→生成逐环打印），定位三个断点：
  1. **提示词缺陷**：scene 判定标准把电脑桌归入 diy-furniture（只说"操作台面=workbench"）→ 电脑桌语义整链未触发。
  2. **高度默认缺陷**：height=null 一律给 750，没有"双层+上方置物 → hutch 总高"推断。
  3. **洞洞板语义缺陷**："中间立面洞洞板"被当成隔板材质，而非立面收纳背板。
- **修复内容**：
  - `extract.ts` 提示词：电脑桌/书桌/办公桌/学习桌明确归 workbench。
  - `intent.ts`：`productType=workbench` 强制桌语义兜底（不依赖 scene）；height 缺省按层数推断（单层=桌面高 740，多层=hutch 中值 1450）；洞洞板任意位置→立面收纳背板；海洋板等未收录桌面材质→按木板兜底（不再降级丢弃）。
  - 工作台开放式约束放宽：允许洞洞板背板（电脑桌常见形态），实体背板/门板/底板仍关闭。
  - `validate.ts`/`App.tsx` 同步豁免洞洞板背板。
- **端到端验证**（同一句话）：抽取 scene=workbench ✓；spec=1500×900×1450、桌面 740 木板、上层浅搁板 55%、洞洞板立面 ✓；浏览器截图确认形态为"下桌面+上展示层+立面洞洞板"的电脑桌 ✓。
- **回归**：`tsc`/`golden`/`build` 全通过。
- **教训**：提示词分类标准的一个措辞（"操作台面"）能让整条领域语义链失效；单句诊断脚本（diag.ts）应保留为常规排查工具。


##### 2026-08-14 项目启动检查 + 纯桌校验档位修正 ✅（commit f2b94c0）

- **启动检查结果**：dev 服务器正常（localhost:5173）；`tsc` 通过；`npm run golden` 全通过（含 M5.2 拓扑不变量：NaN×3 阻断、深度不足阻断、层高冲突阻断、纯桌 740/800 拓扑+val-002）；工作树干净，main @ f2b94c0 与远端同步。
- **发现并修复**：纯桌 720mm 是合法形态（680~800 档位），但 `val-workbench-height` 仍按带上架档位（1100~1800）警告——校验与生成器的双形态语义不一致。修正后纯桌方案状态从"⚠ 1 项警告"变为"✓ 可制造"。
- **校验文案升级**：明确提示两个合法区间"纯桌面 680~800mm 或带上架 1100~1800mm"。


##### 2026-08-14 案例落地第1步：知识库扩充 ✅（commits 3e8c8fa, 341d58f）

- **archetype 意图批（3e8c8fa，此前已验证未提交）**：`FrameSpec.archetype` 字段；shelf→storage-rack / aquarium→aquarium-stand / cabinet≥1800→wardrobe 判定与真实档位默认值；`val-rack-tipover`/`val-rack-depth`/`val-wardrobe-depth`/`val-aquarium-load`/`val-aquarium-height` 档位校验；golden 第4节 archetype 意图回归。
- **知识库扩充（341d58f，来自随构/21 案例库）**：
  - fasteners 17→38 SKU：福马轮GD-40F、调平地脚M8、加长螺母柱、三折/反弹轨道、周转箱148/230、层板托20/30、平面直角连接件、垂直角码、三通本体、内置角槽本体、端面盖板、M4系螺丝/垫片。
  - 连接件 4→6：**内置角槽2020**（真实案例主承重，primary/隐藏/免打孔）+ **三通端面连接件3030**（8角主力，M8端攻×3）。
  - archetypes v0.2：computer-desk 740 腿高标注案例实证；新增 **drawer-tower**（节距160~230 std165、框架高=层数×节距+80、拓扑=正面抽屉+侧洞洞板+福马轮/调平脚）。
- 回归：tsc/golden 全过（知识库完整性 3截面/6连接件/8规则包/38SKU）。
- 计划进度：①知识库扩充 ✅ → ②板材固定按板厚分档（进行中）→ ③mobility三态 → ④纯桌拓扑+golden锚点 → ⑤抽屉拓扑+golden锚点 → ⑥全量回归收尾。


##### 2026-08-14 案例落地第2步：板材固定按板厚分档 ✅（commit 90f7953）

- panels.yaml v0.2：mount 枚举 2→4 档（t-nut-screw / gasket-clamp / **shelf-support 层板托平嵌** / **corner-flat 平面直角件**）；洞洞板改名"自打孔海洋板"、宜家孔制式（Φ5/孔距50/边距10）、固定改 corner-flat；新增 wood-12 平嵌层板数据（供抽屉步使用）。
- generate：`mountFasteners` 统一分派紧固件 BOM（层板托→shelf-support-20/30×4+自攻×8；平面直角件→薄L片×4+M4系）；侧挂薄板用**垂直角码**（真实案例 4mm 海洋板侧挂方案）；Mount.method 直接透传板材档位。
- 回归：tsc/golden/build 全过。
- 计划进度：①✅ ②✅ → ③mobility三态（下一步）。


##### 2026-08-14 案例落地第3步：mobility 三态 ✅（commit faa4942）

- `mobility` 二态→三态：fixed / **leveling-feet（调平地脚）** / caster——真实案例中调平地脚与脚轮同等高频（三抽屉柜/展示柜/边几均用地脚）。
- generate：调平地脚附件（leveling-foot-m8 拧入落地柱 M8 端攻）+ 加工特征派生（end-tap 标注"调平地脚"）+ 成本不重计（foot-stem 归附件项）。
- Viewer：地脚渲染（圆盘垫+丝杆），地面贴底逻辑兼容脚轮/地脚两种垫高。
- assembly：新增"调平地脚"装配步骤（翻转拧入+落地旋调消晃）。
- UI："带脚轮"复选框 → "底部"三选下拉（直接落地/调平地脚/脚轮）。
- 回归：tsc/golden/build 全过。
- 计划进度：①✅ ②✅ ③✅ → ④纯桌拓扑修正+极简桌golden锚点（下一步）。


##### 2026-08-14 案例落地第4步：纯桌拓扑对齐真实案例 + 黄金锚点② ✅（commit f369a1e）

- **纯桌拓扑重构**（对齐随构/21 极简桌实拍 BOM）：腿全高（腿顶=桌面顶，案例 740×4 实证）；桌面**凹嵌**顶框内（新 mode `top-inset`，平面直角连接件×4 下方固定）；**底部长边双撑**（离地≈120，短边开放保证腿部进出）；跨度>1200 顶框自动加**中横梁**（角码固定，Mount 记录）。
- 深度阻断下限 550→500（案例桌深 545 合法；显示器桌 550 警告线保留在 val-workbench-depth）。
- val-002 挠度校验适配凹嵌板（承载梁与板同层）。
- **黄金锚点② golden 用例**：输入 1342×545×740（eu-2020+内置角槽）→ 输出构件必须对上案例 BOM：**740×4 腿 / 1302×4 长梁 / 505×3 短梁 / 凹嵌桌面** ——一次通过 ✓（梁长公式 1342−2×20=1302 与真实产品完全一致）。
- 回归：tsc/golden/build 全过。
- 计划进度：①✅ ②✅ ③✅ ④✅ → ⑤抽屉拓扑+两个抽屉柜锚点（下一步，最大一块）。


##### 2026-08-14 案例落地第5步：抽屉拓扑 + 黄金锚点③ ✅（commit 14b831b）

- **抽屉塔拓扑**（Phase 0.5 第三批遗留项落地，随构/21 三抽屉柜实证）：`drawerCount`/`drawerKind` 进 FrameSpec；每层双深向轨道梁（角码两端固定于柱，案例"角码×12"）；节距=(H−2s)/层数，<120 阻断（案例档位 160~230）。
- **两种抽屉方案**：成品抽屉盒+反弹轨道（家具，无拉手按压开启）/ 周转箱+三折轨道（工具场景）——BOM、装配说明、渲染（箱体+木色前面板）全链路。
- **抽屉不再降级**：intent 把 position=drawer 映射为 drawerCount（默认成品抽屉方案），从"诚实降级清单"移除——用户说"两个抽屉"现在直接生成。
- UI：非工作台场景新增"抽屉层数"滑杆+方案下拉。
- **黄金锚点③ golden 用例**：350×400×490 三抽屉柜（eu-3030+三通）→ 构件对上真实 BOM：**490×4 柱 / 340×10 深向梁（顶2+底2+轨道6）/ 290×4 宽向梁** + 3 抽屉盒 + 4 调平地脚——一次通过 ✓。
- 回归：tsc/golden/build 全过。
- 计划进度：①~⑤ ✅ → ⑥全量回归+文档收尾（最后一步）。
- 注：黄金锚点①（工具收纳柜 670×400×815 双列分区）需要中柱+双列异构拓扑，工作量大，列入下批（已记录在 21-真实案例库）。


##### 2026-08-14 案例落地第6步：全量回归收尾 ✅ 五步计划全部完成

- **全量回归**：tsc ✓ / golden 全过（含黄金锚点②③）✓ / build ✓ / **m5 AI 链路 10 条全跑通**（req-001 valid、req-005 抽屉从"超纲降级"变为直接生成成品抽屉方案）。
- **本批产出总览**（6 commits：3e8c8fa→14b831b）：
  - 知识库：+21 SKU、+2 主承重连接件、+drawer-tower archetype、板材固定 4 档
  - 引擎：mobility 三态、纯桌真实拓扑（凹嵌+底撑+中横梁）、抽屉塔拓扑（双方案）
  - 回归：黄金锚点②（极简桌 1342×545×740）+ 锚点③（三抽屉柜 350×400×490）——生成 BOM 与真实产品完全对齐
  - 意图：抽屉不再降级
- **发现的后续优化点**：req-005"5个抽屉"抽取只给 1 条 drawer panel（数量词未展开）→ drawerCount 应参考 layers/数量词；黄金锚点①双列分区、顶板 recessed、颜色维度、洞洞板 side、预装约束等见 21-案例库待办表。

---

### 2026-08-14 代码审查 + 全量修复 + 待办落地（16 commits）

对全部核心流水线（extract→intent→generate→validate→select）+ 知识库 + 前端做了用户视角深度审查，修了 11 项审查发现 + 完成 4 项待办 + 1 项渲染修复。

##### 审查修复（11 项，P0→P2）

| # | commit | 内容 | 优先级 |
|---|---|---|---|
| 1 | `9626b44` | **调平地脚静默丢弃**——抽取提示词/intent/select 三处补全 leveling-feet，对话主入口终于能识别"调平/地脚" | P0 |
| 2 | `1474247` | **上层搁板 0.58→0.55**——App 默认值对齐知识库，首次加载与 AI 生成不再矛盾 | P0 |
| 3 | `a8339fb` | **选型跨度统一为固定点迭代**——新增 `selectSectionFixedPoint`，从小型材逐一校验真实净跨，物理正确且两处一致 | P1 |
| 4 | `a2e4752` | **JSON.parse 加 try-catch**——LLM 坏 JSON 时给可操作提示+脱敏片段 | P1 |
| 5 | `0745d8c` | **隔板滑杆 min=1**——workbench 场景与强制语义一致，消除"拖到 0 弹回"困惑 | P1 |
| 6 | `22029d6` | **抽屉归零清除 drawerKind + 载荷 min=5**——边界语义清理 | P1 |
| 7 | `bdaa69a` | **machining 改 discriminated union**——去掉 `as` 强转，switch 自动收窄类型安全 | P2 |
| 8 | `7a29c78` | **dMin/dMax→deskTopMin/deskTopMax**——消除与深度变量的命名混淆 | P2 |
| 9 | `a5b87d9` | **抽屉盒宽度公式澄清**——几何分析确认公式正确，加注释/命名常量 | P2 |
| 10 | `99d940d` | **鱼缸系数注释拆分**——系数 1.5 与经验值 200kg 不再混为一谈 | P2 |
| 11 | `229827f` | **数量词展开规则**——"5个抽屉"不再只抽出 1 条 | P1 |

##### 待办落地（4 项）

| # | commit | 内容 |
|---|---|---|
| 12 | `5b02b72` | **中柱 + 双列分区拓扑**（黄金锚点①骨架）——`centerColumn` 模式：中心 x=0 前后两根全高立柱，横梁断开为左右两段，将内腔分为左右双列；已加 UI 勾选框 + golden 测试 |
| 13 | `30f64d7` | **顶板凹陷嵌框模式** `top-recessed`——`topPanelMode` 维度（overlay/recessed），储物柜顶板嵌框内坐落在框梁上，不悬挑 |
| 14 | `8a8ae39` | **洞洞板 side 侧挂语义**——pegboard + position='side' 不再映射为背板，挂于左立面（工具墙收纳语义） |
| 15 | `c3d4416` | **滑块/弹珠螺母预装约束**——装配说明的"识别与清点"和"底框与立柱"步骤，当连接件用 T 型螺母时加入预装提示与警告 |
| 16 | `afc14bc` | **型材颜色三档**——silver/black/gold 阳极氧化调色板，Viewer 材质渲染 + UI 选择器 |

##### 板材渲染修复（1 项，多轮迭代）

| commit | 内容 |
|---|---|
| `5736f43` | 板材程序化纹理初版（洞洞板孔阵/围网网格/木纹）——反馈"效果差" |
| `fc79f00` | 纹理真实化重写——基于真实规格（洞洞板 Φ5@25mm、木纹长程纤维） |
| `5ce44cd` | 纹理按真实尺寸 repeat + 洞洞板孔透明（alphaMap） |
| `0f7b1ff` | **UV 按物理尺寸编码**——废弃 BoxGeometry，自定义 `panelGeo()`，UV=物理位置/400mm，解决拉伸和朝向不一致 |
| `8d43a41` | 木纹无缝平铺（sin 替代线性渐变）——反馈"全是缝子" |
| `1a5076c` | **去掉木纹**——假纹理比无纹理难看，wood 改干净实木色 `#b58b54`；洞洞板/围网保留（功能性纹理） |

##### 关键经验教训

1. **程序化 Canvas 木纹极难做好**：在有限画布上用 2D 绘制模拟真实木纹，必然遇到接缝/拉伸/重复感三重问题。结论：Phase 0 不做木纹，靠材质参数（颜色/粗糙度/反射）体现质感；如需真实木纹，未来走贴图资源（需外部图片文件，不在 Phase 0 范围）。
2. **BoxGeometry UV 不适合纹理面板**：其 UV 每面独立 0-1，同一 repeat 在不同朝向/大小面上纹理密度完全不同。改用自定义 BufferGeometry，UV 直接编码物理尺寸。
3. **洞洞板必须透明**：孔是贯穿的，alphaMap 通道（孔=黑透明，板=白不透明）让孔真正透光。

##### 当前状态

- tsc ✓ / golden 全过（含中柱拓扑、顶板凹陷、洞洞板侧挂、装配预装、型材颜色、纹理渲染、数量词联动 7 个新测试）✓ / build ✓
- 项目待办（CLAUDE.md 记录的"下一批"）**全部完成**：黄金锚点①骨架（中柱拓扑）、顶板 recessed、颜色维度、洞洞板 side、预装约束、数量词解析
- 板材渲染现状：木板=干净实木色（无纹理）、洞洞板=孔阵+凹凸+孔透明、围网=钢丝网格+凸起、玻璃/亚克力=半透明

---

### 2026-08-14 渲染修复 + 数量词联动 + 锚点①评估（7 commits）

##### 板材渲染多轮迭代（反馈驱动）

| commit | 内容 | 反馈 |
|---|---|---|
| `5736f43` | 板材程序化纹理初版（洞洞板孔阵/围网网格/木纹） | "效果太差了，跟现实完全一点关系都没有" |
| `fc79f00` | 纹理真实化（真实规格 Φ5@25mm、木纹长程纤维、seed 稳定） | — |
| `5ce44cd` | 纹理按真实尺寸 repeat + 洞洞板孔透明（alphaMap） | — |
| `0f7b1ff` | **UV 按物理尺寸编码**——废弃 BoxGeometry，自定义 panelGeo()，UV=位置/400mm | — |
| `8d43a41` | 木纹无缝平铺（sin 替代线性渐变） | "现在全是缝子了" |
| `1a5076c` | **去掉木纹**——假纹理比无纹理难看，wood 改干净实木色 | "做不到就把木纹去掉" |
| `c332bcc` | 洞洞板去随机噪点（背板和侧板颜色一致） | "背板和侧板颜色不一致" |

**最终方案**：木板=纯色实木感（无纹理）、洞洞板=规则孔阵+凹凸+孔透明、围网=规则钢丝网格

##### 数量词与抽屉联动（commit `70d74ae`）

- intent 层新增：cabinet + layers>1 + 无 drawer 条目 → layers 作为抽屉数
- 有 drawer 面板条目 → shelfCount=0（抽屉塔不生成搁板）
- golden 测试：5层→5抽屉 ✓；显式drawer→1抽屉+0搁板 ✓；3层架→2搁板 ✓

##### 黄金锚点①评估（commit `84c5cd0`）

从 [[21-真实案例库]] 提取工具柜 670×400×815 完整 BOM：
- 非均匀双列：左列 425（周转箱抽屉）+ 右列 185（工具挂板）
- 完整 BOM：630×4 / 185×2 / 810×4 / 775×2 / 360×14 + 内置角槽×30 + 周转箱×4
- **差距**：当前中柱拓扑为对称均分（梁在中心断开为半段），真实产品需非均匀双列+通长横梁
- 评估结论：Phase 0 后拓扑扩展项，中柱骨架已就绪

##### 关键经验教训

1. **程序化木纹不可行**：有限画布 2D 绘制无法模拟真实木纹（接缝/拉伸/重复感三重困境）。Phase 0 不做木纹，靠材质参数体现质感
2. **BoxGeometry UV 不适合纹理面板**：每面独立 0-1，同一 repeat 在不同朝向面上密度不同。改用自定义 BufferGeometry，UV 编码物理尺寸
3. **洞洞板必须透明**：孔是贯穿的，alphaMap（孔=黑透明，板=白不透明）让孔真正透光
4. **非均匀双列是真实产品标配**：黄金锚点①实证——真实抽屉车是异构拓扑（不同列不同内部结构），不是均匀网格

---

### 2026-08-15~08-18 中柱分区拓扑 + 三种列类型 + 几何修正（10 commits）

> 本轮目标：工具柜/异构柜的双列分区拓扑从骨架到完整可用，支持抽屉/隔板/柜门三种列类型任意组合。

##### 中柱拓扑 v2——可偏置+左右异构（commit `4cea190` → `3fce277`）

- `centerColumn` 模式在框架中心(x=0)加前后两根全高立柱，将内腔分为左右双列
- 支持 `offsetRatio` 偏置（0.5=居中 / 0.67=左2/3 / 0.33=右2/3），非均匀双列
- 横梁(beam-x)在中心处动态断开为左右两段，各连接角柱与中柱（commit `465fee2`）
- 左右两列可独立配置异构结构（左抽屉+右柜门等）

##### 分区抽屉盒宽度和位置按分区计算（commit `c8b016e`）

- 抽屉盒不再用全宽 `W−2s`，改为按所在列净宽 `colWidth−DRAWER_SIDE_CLEARANCE` 计算
- 位置锁定到列中心 `xCenter`，不再跨列

##### 中柱分区支持三种列类型——抽屉/搁板/柜门（commit `be550ff`）

- `CenterColumnType = drawer | shelf | cabinet`
- `addCenterColStructure` 辅助函数统一处理三种列类型
- `cabinet`（柜门）：内部可选搁板层数 + 正面门板（铰链×2+磁吸+把手，左铰右开）
- UI 下拉新增"柜门"选项（App.tsx `CenterColumnConfig`）
- types.ts：`left/right` 类型从联合类型改为 `CenterColumnType`

##### 中柱分区解耦+隔板材质+柜门缝隙修复（commit `b35f805`）

- 中柱模式跳过旧抽屉塔代码（`!spec.centerColumn`），双路径解耦
- 隔板面板材质跟随 `spec.shelfPanel`
- 柜门缝隙初步修复

##### 中柱分区几何修正——柜门重叠立柱+隔板居中（commit `8b1ff30`）

- 柜门宽度 `colWidth → colWidth + 2s`（两侧各重叠立柱 s，消除缝隙）
- 隔板面板居中于所在列 `colCenterX`（非框架中心 x=0）
- 隔板宽度=列内净宽 `colWidth−s`，深度=内深+30mm 搭梁
- left/right 可选，支持空/抽屉/隔板/柜门任意组合

##### 中柱分区列中心偏移 s/2 修正（commit `7722d7d`）

- **Bug**：`colCenterX = colXLeft + colWidth/2` 偏移 s/2（2020 系=10mm）
  - 因为 `colWidth` 是柱间净宽 = 立柱中心距 − s，直接加半宽会偏向立柱中心中点 s/2
  - 后果：柜门重叠立柱不足（边缘只到柱中心，应到柱边+s，残留缝隙）；抽屉/隔板在列内不居中
- **修复**：`colCenterX = (colXLeft + colXRight) / 2`（真正两立柱几何中点）
- 修复后：门精确重叠立柱 s=20mm，抽屉/隔板居中于列
- 用户反馈驱动："玻璃门仍然和中轴存在距离差异"+"抽屉占据全部没有分区"

##### 诊断验证

- 编写多场景诊断脚本（drawerCount×centerColumn 组合、门/抽屉/隔板几何）
- 确认数据层分区逻辑正确（空列无抽屉、中柱模式跳过旧塔）
- 三个反馈中仅"玻璃门偏移"为真实几何 bug；"抽屉占据全部"+"空列仍有抽屉"在数据层已正确（可能为旧版残留或视觉误判）

##### 关键经验教训

1. **净宽≠中心距**：柱间净宽 = 立柱中心距 − s，用净宽反推列中心时必须回到立柱中心坐标，不能 `colXLeft + colWidth/2`
2. **门重叠立柱的精确几何**：门宽 = 柱间净宽 + 2s 时，必须居中于立柱中心中点，才能两侧各重叠 s；任一偏差都会残留缝隙
3. **分区模式需彻底解耦**：中柱模式必须跳过旧的全宽抽屉塔代码路径，否则双路径会叠加出诡异结果

##### 当前状态

- tsc ✓ / golden 全过（含中柱拓扑、顶板凹陷、洞洞板侧挂、装配预装、型材颜色、纹理渲染、数量词联动、黄金锚点①对齐 8 个测试）✓
- 中柱分区功能完整可用：三种列类型 × 左右异构 × 偏置非均匀双列


##### 2026-08-18 Claude Code 迭代批质量审查 + 全量推送 ✅（commits c3c3b9b, b106349）

- **背景**：用户用 Claude Code（LongCat-2.0 接管）本地迭代了 58 个提交——把上批待办全部做完：中柱分区拓扑（三列类型：抽屉/搁板/柜门、可偏置、左右异构）、黄金锚点①工具柜、数量词联动（5层→5抽屉）、顶板 recessed、洞洞板 side、颜色三档、板材纹理、预装约束。golden 已扩到 12 节全过。
- **独立代码审查发现 3 个确定性问题并修复**（c3c3b9b）：
  1. 柜门列内部隔板误用全宽 addPanel（横穿中柱侵入邻列）→ 抽出 `addColShelfPanel` 列宽自建板，cabinet/shelf 共用；
  2. 中柱抽屉列节距无下限校验（负尺寸抽屉盒风险）→ pitch<120 阻断；
  3. 样例按钮浅合并残留旧 centerColumn → `setSpec` 完整替换；
  4. 顺手：中柱内端 joint 位置 xCenter±s → ±s/2。
- **docs 导入**（b106349）：Obsidian 随构/ 22 份文档全量复制进 `docs/随构/`（推送前扫描无明文密钥）+ `.claude/plans/center-column-v2.md`；`.claude/settings.local.json` 加 gitignore。
- **注意**：docs/ 是快照副本，Obsidian 仍是唯一事实源——后续改动需手动重新复制同步。
- 回归：tsc + golden（12 节含三个黄金锚点）全过后推送，main @ b106349 与远端同步。


##### 2026-08-18 待办三连清：爆炸图 + DXF 导出 + 多句记忆 ✅（commits 65f71c4, 5e6b751, fb8fdb6, a1b0129）

- **爆炸图**（65f71c4）：视图工具条下新增"💥 爆炸"滑杆（0~100%），形心乘性外扩（Y 向 1.5 倍加权拉开层次），数据层实现 Viewer 零改动；爆炸时自动隐藏接点/孔位/标注/尺寸避免错位；图纸模式不可用。
- **DXF 导出**（5e6b751）：新建 `src/engine/dxf.ts`，R12 ASCII 格式——切割清单全件号纵向平铺，每件轮廓矩形+孔位圆（端面攻丝画端部十字圆）+件号文字标注；顶栏新增 DXF 按钮（走导出闸门）；golden 第 13 节断言（头尾结构/圆数≥孔数/标注数=件号数）。
- **多句对话记忆**（fb8fdb6）：历史窗口 6→10 轮；方案状态 JSON 补全 drawerCount/drawerKind/centerColumn 摘要/profileColor/archetype；**降级历史回传**（此前降级项列表注入上下文，用户再提及时模型可正确应答）。
- 回归：tsc/golden（13 节）/build 全过；每项独立 commit 已推送，main @ a1b0129。
- **剩余待启动**：STEP 导出（需 3D 内核，评估 occt-import-js）、变高立柱（锚点①真实产品外柱810/内柱775）、2040 矩形梁。


## 2026-08-19（续4）：变高立柱落地——锚点①与真实 BOM 完全对齐

**commit e8eacf8** `feat(engine): variable-height center posts with full-length top beams matching real cabinet BOM`

### 规则反推（真实工具柜 BOM 810×4 + 775×2 实证）
- 三视图标 815，BOM 立柱 810 → 取 **H=810** 为真实高度（5mm 视为量测差），公式随即精确成立：
  - 外柱 = H 全高 = **810** ✓
  - 顶梁**通长不断开**（架在中柱顶上），顶梁中心 = H − 板厚 − s/2
  - **中柱 = H − 顶板厚(15) − 梁高(20) = 775** ✓
  - 顶板上表面 = H，与外柱顶齐平（top-recessed 语义在中柱场景的自然延伸）

### 实现（generate.ts）
- `centerTopPanelT`：仅 centerColumn + topPanel≠none + topPanelMode='recessed' 时 = 板厚，否则 0
- 中柱 addPost 到 `H − centerTopPanelT − s`；`centerTopBeamY = H − centerTopPanelT − s/2`
- `addRectLayer` 增加 `{split}` 选项：顶层梁强制通长（不在中柱处断开），底层/中间层照旧断开
- 顶板 recessed 落在下沉后的梁顶（`H − centerTopPanelT`）

### 锚点①断言升级（golden 第12节）
- 输入：height 815→**810**，增加 `topPanelMode: 'recessed'`
- 断言：外柱 **810×4**、中柱 **≈775×2**（wood 18mm 时 772，容差±5）、顶层通长 **630×2** 横梁、总柱仍 6
- tsc ✓ / golden 13节全过 ✓ / build ✓

### 待办更新
- ~~变高立柱~~ ✅ 剩：2040 矩形梁、STEP 导出评估


## 2026-08-19（续5）：2040 矩形梁——梁柱双截面落地

**4 个提交**：ad0aea0（知识库）→ 2374dc3（矩形渲染）→ e523bfb（生成器/校验）→ 0866254（UI+golden第14节）

### 设计：梁柱双截面（方案 A）
- FrameSpec 新增 `beamSectionId?`：柱保持 2020 方形，**层框梁**可选 eu-2040 立放（宽20=柱宽槽对齐，高40 强轴抗弯 ix 6.0e4 ≈ 2020 的 8.6 倍）
- **对齐规则**：底框底对齐（梁底=0，底板改坐梁顶=40）；其余层顶对齐（梁顶/板位不变，中心下移 beamDrop=(bh−s)/2）——方形梁 beamDrop=0 全兼容
- 特殊撑杆（桌底双撑/后撑/抽屉轨道梁）**不升级**，保持柱截面
- 两类非法组合诚实阻断：梁宽≠柱宽（3030柱+2040梁）；2040+抽屉塔/中柱分区（内部 y 基准待扩展）

### 知识库
- `eu-2040.yaml`（inferred）：size [20,40]、40宽面双槽 offset±10、双芯孔 coreHolePositions [[0,±10]]、ix 6.0e4/iy 1.4e4、25元/m
- 20系连接件（corner-cube-20/internal-slot-20/vertical-bracket-20）compatible.series 加 eu-2040

### 渲染（profileGeometry 重写）
- buildSectionShape 矩形化：**数据驱动 faces 槽位**（每边按 offsets 生成 T 槽锯齿）+ 多芯孔；梁 axis 旋转天然使"截面高方向=世界Y"立放，Viewer 零改动

### 工程链路
- 切割清单按件截面分组（partKey 加 sectionId，去掉硬编码 sec.id）——BOM/DXF 自动正确
- validate 挠度用 beamSec.ix + loadBeamY 用梁高；UI 加"梁截面"下拉（柱下拉过滤掉矩形截面）
- golden 第14节：1342 跨 60kg 用 2040 挠度通过、顶对齐 y=720、撑杆不升级、清单分截面、两类阻断

### 待办更新
- ~~2040 矩形梁~~ ✅ 剩：STEP 导出评估


## 2026-08-19（续6）：STEP 导出方案评估（调研完成，待实施）

### 候选对比（GitHub 实查 2026-08）
| 方案 | 结论 | 依据 |
|---|---|---|
| occt-import-js | ❌ 排除 | 只有 ReadStepFile/ReadIgesFile/ReadBrepFile，**纯导入无导出**；且 2 年未更新 |
| opencascade.js 直接绑定 | ⚠️ 次选 | 完整 OCCT 绑定含 STEPControl_Writer，但停滞 3 年（v2.0.0-beta，OCCT 7.6.2），减体积需 Docker 自定义 build，维护风险高 |
| **replicad** | ✅ **首选** | MIT；**4 天前刚发 v1.0.0**（迁 OCCT v8 绑定）；高层 API（drawing→sketch→extrude→exportSTEP）；专为浏览器集成设计（Web Worker 友好）；672★ 活跃 |
| 后端转换（pythonocc/cadquery） | ❌ Phase 0 无后端 | 留作 Phase 1+ 备选 |
| glTF/STL（three.js 内置 exporter） | 💡 廉价过渡 | 零成本 mesh 导出可先行，但加工厂要 B-rep STEP，不可替代 |

### 实施蓝图（下一任务）
1. `npm i replicad replicad-opencascadejs`；wasm（~10-15MB gzip）**惰性 import()**——点"导出 STEP"才加载，Web Worker 中初始化避免卡 UI
2. FrameModel → STEP：型材件复用 faces 数据驱动的 T 槽轮廓点列（与 profileGeometry 同源）→ replicad drawing → extrude 长度 → 按 member position/axis 平移旋转；板材 = box；全部 compound → exportSTEP blob 下载
3. 孔位 Phase 0 第一版可只给外形+槽（钻孔信息已有 DXF 单件图承载）；后续可 boolean 减孔
4. 复用 exportGate（invalid/incomplete 禁止导出）
5. 风险：wasm 加载失败降级提示"用 DXF+切割清单"；公司网络离线缓存（HTTP cache/IndexedDB）


## 2026-08-19（续7）：爆炸图连接件跟随修复

**commit 0bc4791** `fix(ui): exploded view moves joints with their beams instead of hiding them`

- 问题：爆炸时连接件（角码/joint）直接被隐藏（joints 传空数组），装配爆炸图缺少连接件层次
- 修复：`shownJoints` = joint 位置 + 所属梁（`beamMemberId`）的爆炸位移增量——**角码装在梁端的装配语义**，随梁一起飞出，与柱脱开
- 标注类（machining/mounts/dims/bubbles）爆炸时仍隐藏（合理，非实体件）
- tsc ✓ golden ✓


## 2026-08-19（续8）："AI 生成和描述完全不沾边"诊断修复

**commit cf1e6c2** `fix(ai): new product type resets manual locks; unlisted materials fall back to wood; drawer cabinet estimates tiers by height`

### 复现（diag.ts 支持命令行句子后）
输入"45立方极简美学铝型材海洋板亚克力抽屉柜 H,D,W:45CM"→ 抽取层其实**正确**（cabinet 450³ + side:other + drawer:acrylic），断在下游三处：

### 三个断点与修复
1. **手动锁定污染（主因）**：用户此前手动调过 8 项板材（wire-mesh/glass 门板等），锁定项持久化在 localStorage 草稿；新描述没显式提到的字段全部回填旧值 → 展示出与描述无关的大杂烩。修复：**productType 变化 = 全新物件** → 自动清空 manualChanges + unsupportedSaved，不做回填，回复加"🔓 已解除此前手动锁定"（lastProductType 持久化到草稿）
2. **未收录材质降级丢弃**：海洋板(other) 侧板在非 workbench 场景直接进 unsupported。修复：top/shelf/side/bottom 未收录材质**全场景兜底木板**+假设说明（门板除外，需精确材质）
3. **"抽屉柜"只出 1 层**：一条 drawer 记录=1 层。修复：层数未说明时按 高度÷节距205（案例档位 160~230 中值）估层，450→2 层

### 修复后
降级=[]、backPanel=wood、抽屉 2 层、状态 valid。golden 14 节全过。
diag.ts 现支持 `npx tsx scripts/diag.ts "任意句子"`。


## 2026-08-19（续9）：工具柜案例翻车修复——柜类场景防线 + 中柱双列映射

**commit 2d64175**（golden 新增第 15 节意图防线）

### 案例
"铝型材工具柜带滑轮 整体大小约为 1m*40 高度为90 两个抽屉 一个大的柜子侧开门 侧板带挂架功能"
→ 修复前：LLM 把 scene 判成 workbench → 整柜按电脑桌语义：抽屉×2 全降级、门板/侧板被开放式清空、深 400 被强制 550、高 900 落进电脑桌 801~1099 死区直接报错。

### 三处修复
1. **确定性防线（intent.ts）**：productType ≠ workbench 时 scene=workbench 一律修正为 diy-furniture——柜/架类绝不进电脑桌语义，LLM 误判无法穿透
2. **中柱双列映射（intent.ts 新路径）**：drawer×N + door + cabinet → `centerColumn{offsetRatio 0.4, 左列抽屉×N, 右列柜门}`（锚点①实证拓扑），普通抽屉塔计数归零防双重生成、doorPanel 交分区列
3. **提示词强化（extract.ts）**：规则5 显式"工具柜/收纳柜/抽屉柜等一切柜类=diy-furniture（柜类绝不是 workbench）"；规则15 补"挂架/挂钩/挂工具立面=pegboard"

### 修复后 diag
scene=diy-furniture ✓ 深 400 保留 ✓ 6 立柱（2 中柱 870 变高）✓ 左列 2 抽屉+右列 door-front 609 宽 ✓ 洞洞板侧挂 ✓ 降级=[] ✓
golden 15 节全过（第 15 节 = 本案例 mock 抽取端到端断言，防回退）。


## 2026-08-19（续10）：柜体封闭语义修复（用户截图指出两处硬伤）

**commit f6bbf3c** `fix(ai): door cabinets enclose all sides automatically; cabinet column defaults to no internal shelf`

### 用户指出（完全正确）
1. "门板就是一个门板"——门是一块悬空板，门后柜腔无背板/顶板/底板/侧板，不是封闭空间
2. "门板后面为什么还有隔板用的边横柱"——intent 映射写死 cabinet count=1，生成了没人要的内部搁板+横梁

### 修复（intent.ts）
- **柜体封闭语义**：带门柜（centerColumn cabinet 列或 doorPanel）自动补齐 顶/底/背/左侧/右侧 板（木板，已指定材质的面保留如洞洞板）+ 假设记录；rightPanel 从硬编码 'none' 变量化全链路打通
- **柜门列 count 1→0**：用户未提内部搁板则纯空柜（cabinet count=0 → 无搁板横梁）

### 浏览器端到端验证（同句重发）
正面：右列大门（带把手，门后干净无横梁）+ 左列 2 层抽屉面板；背面/侧面/顶面全封闭；左侧洞洞板保留；6 脚轮。板材清单：门 612×820 + 顶 1000×400 + 底 950×350 + 背 1000×900 + 右侧 400×900 + 洞洞板 400×900，合计 ¥992。
golden 第 15 节补断言：柜体四周封板 + 柜门列 count=0。15 节全过。


## 2026-08-19（续11）：板材接缝工艺 + 门/抽屉前脸共面

**commit 66c2cca** `feat(engine): panel joinery - top caps side/back edges, sides cap back, coplanar drawer fronts and doors`

### 用户问题
1. 板材与板材连接处的缝子怎么处理？（顶板与侧板台阶缝、背板与侧板竖角开缝）
2. 门板和抽屉不在一个平面（门凸出正面 D/2，抽屉盒缩在框内 D/2−s）

### 搭接次序（家具封边工艺，generate.ts）
- **顶板盖全**：overlay 顶板尺寸扩为 (W+左侧厚+右侧厚)×(D+背板厚)，盖住侧板/背板顶端面——接缝藏顶板底面，防尘防水
- **侧板盖背板**：侧板深度 D+背板厚 向后延伸，竖角缝转到侧面藏住
- mountNote 注明"接缝封边条或密封胶处理"
- workbench 场景不参与（桌类无围板）

### 前脸共面（正面一张脸）
- **成品抽屉（ready-made）新增前脸板** mode `drawer-front`：z = D/2+t/2 与柜门完全共面，盒内 M4×4 反锁（家具标准），无拉手按压反弹开启
- **缝落柱中线**：门宽/前脸宽统一改"柱中心距 −3mm 缝"（原门宽 colWidth+2s 会与邻列重叠 s）——相邻面板中缝正对立柱中线，缝后有柱不透光
- 周转箱（turnover-box）保持开放无前脸（工具场景）

### 浏览器验证（同句重发）
正面=一张平整脸：左列上下两块前脸板+右列大门带把手，三块板共面、缝均匀；顶板 1023×418 盖侧背、侧板 418 深含背板厚；BOM 新增 B7 前脸 385×407×2。golden 15 节全过。


## 2026-08-19（续12）：五金硬件真实化（用户要求"真实能用"）

**commit 15bb6f7** `feat(hardware): real drawer slides, hinges, handle, magnetic catch as 3D entities`

### 用户指出缺失
抽屉与前脸怎么连接、抽屉怎么打开（要指拉缝）、门把手位置、门合页、抽屉滑轨——全都没有实体

### 落地内容
1. **抽屉前脸修正**：底缘齐层底、**上缘留 20mm 指拉缝**（无拉手可指拉可反弹按压）；盒内 M4×4 反锁前脸（家具标准）；修正前脸中心与盒体错位 22mm 的 bug
2. **滑轨实体**：每抽屉一副（accessory kind `drawer-slide`，渲染左右两条金属轨）；顺带修复滑轨漏计价（drawer-slide mount fasteners 被排除计价后 accessory sku 补上）
3. **合页×2**：门左缘上下 1/5、4/5 处金属块实体（计价仍走 mount fasteners，accessory sku 空防重计）
4. **把手**：门右缘竖 U 形（孔距 96 两脚+横杆圆柱），位置与 mount 孔位一致
5. **磁吸扣**：右柱中部小块
6. **删除 drawer-box 渲染自带的假前面板**（就是"那个小板"，之前与真前脸重复且不共面）
7. BOM 附件行跳过空 sku 视觉硬件

### 坑：TDZ
accessories 数组声明在 addCenterColStructure 之后，门硬件 push 报 "Cannot access before initialization" → 声明提前到 panels/mounts 处。

### 浏览器截图验证
门带竖把手 ✓ 前脸×2 带指拉缝 ✓ 面板共面 ✓ 滑轨/合页在结构内 ✓。golden 15 节全过。


## 2026-08-19（续13）：门开向可调 + 撤指拉缝

**commit 5743e42** `feat(door): adjustable hinge side; drawer front uses natural 4mm inter-tier gaps`

- **门开向**：CenterColumnType cabinet 加 `hinge?: 'left'|'right'`（默认左铰右开）；生成器按开向镜像布置合页/磁吸/把手（把手永远在开启侧门缘）；UI 中柱分区面板列类型=柜门时出现"左铰右开/右铰左开"下拉
- **撤 20mm 指拉缝**（用户指正：层间本来就有缝）：前脸恢复居中于层区间，高 pitch−4（上下各 2mm 工艺缝），开启方式=反弹轨按压
- tsc ✓ golden 15 节 ✓


## 2026-08-19（续14）：右铰左开双把手 bug 修复

**commit e745dae** `fix(door): remove legacy hardcoded right-edge handle in viewer; handle holes follow hinge side`

- 用户切"右铰左开"后出现两根把手：新 accessory 把手正确移到左缘，但 Viewer 里 door-front 板渲染残留**旧的写死右缘内置把手**（上一代实现）→ 删除，把手统一由 accessory 渲染（随开向镜像）
- 门板把手加工孔（Φ5×2 孔距96）同步随开向镜像（原写死右缘）
- 浏览器截图确认：单把手在开启侧、合页在铰链侧 ✓


## 2026-08-19（续15）：抽屉横拉手（"下面的抽屉怎么开"）

**commit 569affd** `feat(drawer): horizontal pull handles on drawer fronts (96mm pitch, in BOM)`

- 用户追问：底层抽屉怎么开——4mm 层间缝伸不进手指，反弹轨视觉上不可见，不可用
- 每个抽屉前脸加**横向拉手**（孔距 96 标准）：实体渲染 + 前脸板 Φ5×2 拉手孔 + BOM（handle-96 计入前脸 mount fasteners）
- Viewer handle 渲染按 boxSize 宽高比自适应横置（抽屉）/竖置（门）
- 截图确认：左列两抽屉各带横拉手、右列门左缘竖把手（右铰左开）——完整可用工具柜形态


## 2026-08-19（续16）：爆炸图硬件跟随宿主

**commit 2410a24** `fix(explode): hardware accessories follow their host panel/box via hostId`

- 问题：爆炸时门飞出，但把手/合页/磁吸按自身坐标独立乘性外扩（把手 x≈0 几乎不动）→ 与门视觉分离
- 修复：AccessoryItem 加 `hostId?`（宿主 panel/accessory id），生成时挂宿主（合页/把手/磁吸→门板、抽屉拉手→前脸板、滑轨→抽屉盒）；App 爆炸位移 = 宿主中心位移增量（与 joints 跟随梁同款方案）；脚轮/地脚/LED 无宿主保持原逻辑
- 截图确认：门带着把手+合页×2 整体飞出、前脸带拉手、盒带滑轨 ✓


## 2026-08-19（续17）：STEP 导出落地（replicad）+ 第三方验证

**commit 6b25952** `feat(export): STEP export via replicad worker`

### 实现
- `npm i replicad replicad-opencascadejs`（v1.0.0，wasm 单文件 dist/replicad_single.wasm）
- **共享轮廓模块** src/engine/sectionOutline.ts：从 profileGeometry 提取纯数据轮廓点列+芯孔（THREE 渲染与 STEP 导出同源，消除双份维护）
- **Worker**（src/export/stepWorker.ts）：`opencascade({locateFile: wasm?url})` 惰性初始化 → 每根型材 draw(pts).close().cut(芯孔圆) → sketchOnPlane('XY').extrude(length) → 按 axis/tilt 旋转 → translate；板材 drawRoundedRectangle 挤出；`compoundShapes(...).rotate(90,X)`（Y-up→Z-up）→ `blobSTEP()`
- 主线程 src/export/step.ts：Worker 惰性创建、轮廓按截面缓存、Promise 封装
- App"STEP"按钮：exportGate + stepBusy 防重入，动态 import 不进主包

### 验证
- 工具柜方案导出 3.08MB application/step
- **3dviewer.net 第三方验证**：OCCT 8.0 translator 正常解析；单位毫米；外包 1023×436×918mm 与设计精确一致（1000+洞洞板5+侧板18 / 400+背18+门18 / 900+顶18）；每根型材独立实体（点选单件 4040×320）；正面视图抽屉前脸/门板分缝清晰
- 首次导出需加载 wasm 数秒（OCCT Transfer Write 日志可见），此后秒出

### 待办
- CLAUDE.md 三大待办全部清空（变高立柱/2040梁/STEP导出）


## 2026-08-19（续18）：M5 回归 + 装配说明补硬件步骤

### M5 AI 链路回归（今日改提示词+意图层后的纪律性验证）
- `npm run m5` 10 条全部完成 → out/m5-report.{json,md}，无回归
- req-008（今日工具柜句）在测试集内跑通；req-010（45立方抽屉柜）valid；req-006（三段式歧义数字）正确保持 null+追问
- 校验以 pass 为主，无 error 阻断

### 装配说明补硬件（commit 35972de）
- 新增"抽屉前脸与拉手"步骤（drawer-front mount 此前不进任何步骤）：先点固调缝（与门共面、层缝均匀）再 M4 反锁 + 拉手孔距96 安装
- "4 根立柱"硬编码 → 实际柱数（中柱场景 6 根）
- tsc ✓ golden 15 节 ✓

### 后续建议清单（已与用户对齐优先级）
1. ~~M5 回归~~ ✅ 2. ~~装配说明硬件步骤~~ ✅ 3. 部署试用（Vercel/Pages 收真实反馈）
4. STEP 零件命名（exportSTEP ShapeConfig）5. 手动锁定单项解锁 UI 6. 三列分区 7. 2040×抽屉/中柱组合解锁 8. 案例库剩余锚点（鱼缸架/衣柜）9. 洞洞板挂钩 SKU


## 2026-08-19（续19）：GitHub Pages 部署

**commits d94a722**（生产端点+base 路径）+ gh-pages 分支（构建产物 7.25MB 含 replicad wasm）

### 决策与实现
- 平台：GitHub Pages（用户选定，仓库公开）
- vite base：`process.env.GITHUB_ACTIONS ? '/suigou/' : '/'`；本地发布用 `$env:GITHUB_ACTIONS='true'; npm run build` 后 dist 内 git init + force push 到 gh-pages
- LLM 端点：生产静态站直连 https://api.longcat.chat（dev 仍走 vite 代理）；fetch 网络/CORS 失败给友好提示（"AI 理解请本地运行；手动面板与样例不受影响"）；key 仍由访问者自填 localStorage，不进构建产物
- **坑：PAT 无 workflow scope** → 无法推 .github/workflows/deploy.yml，Actions 自动部署挂起（文件改名 .github/deploy.yml.pending 保留）。将来给 PAT 加 workflow 权限后改回即可启用（含 golden 门禁的自动部署）
- 用户待办：Settings→Pages 选 gh-pages 分支；地址 https://guorongxue.github.io/suigou/

### 手动发布流程（记录）
```powershell
$env:GITHUB_ACTIONS='true'; npm run build; $env:GITHUB_ACTIONS=$null
cd dist; git init; git add -A; git commit -m deploy
git push -f https://github.com/GuorongXue/suigou.git HEAD:gh-pages; cd ..
```


## 2026-08-19（续20）：正式上线 🎉

**https://guorongxue.github.io/suigou/**

- 用户给 PAT 加 workflow 权限 → deploy.yml 推送成功（commit 215dea4），Pages Source 切为 GitHub Actions
- **CI 流水线**：push main → npm ci → **golden 15 节回归门禁** → build → 发布 Pages（golden 不过不发布）
- 线上实测：页面加载正常（/suigou/ 子路径资源 ✓）、首次使用 API Key 自填提示 ✓、参数面板/3D/校验/切割清单/下料/价格/装配/STEP 按钮全部工作 ✓
- Phase 0 至此具备完整对外试用条件：默认工作台方案 15 根 ¥283 即开即用，无 Key 也能用全部手动功能


## 2026-08-19（续21）：清单三连（STEP 命名 / 锁定单项解锁 / 洞洞板挂钩）

- **495a861** STEP 零件命名：exportSTEP ShapeConfig 逐件命名"件号 截面 长度"（如 P1 eu-4040-s8 L900）+ 基础色（型材银灰/板材木色），本地实测 PRODUCT 名称写入 ✓——工厂 CAD 打开即知件号对应
- **80c456c** 锁定单项解锁：锁定列表变 chips，每项 × 单独解锁 + "全部解锁"，不再只能靠"新建"或换产品类型
- **52308e3** 洞洞板挂钩：`pegboard-hook-kit`（10 只混装 ¥12）SKU 入库，侧挂洞洞板 mount 自动附带进 BOM——挂架功能有了本体配件
- 全部 tsc ✓ golden ✓ 已推送（CI 自动发布线上）

### 剩余清单
- 三列分区（真实工具柜"抽屉+开放+柜门"三列）
- 2040 梁 × 抽屉/中柱组合解锁（内部 y 基准扩展）
- 案例库剩余锚点（鱼缸架/衣柜）


## 2026-08-19（续22）：三列分区落地（N 列泛化）

**commits 43c26f7**（引擎+校验+golden16）+ **de35951**（UI）

### 引擎泛化（generate.ts）
- FrameSpec 新增 `partitions?: { ratios: number[]; cols: (CenterColumnType|null)[] }`（N 列 = N−1 根中柱）；centerColumn 保留为两列特例，内部归一为 partitions（全兼容，锚点①两列断言不变）
- addRectLayer 分段梁泛化：cuts=[左角柱,...中柱,右角柱] 循环生成 N 段，角柱端缝在框缘、中柱端缝在柱面
- 列结构循环调用 addCenterColStructure（函数无需改）；变高中柱/顶梁通长/2040阻断/抽屉塔阻断条件全部改 partitions
- 合法性：ratios 与 cols 等长、≥2 列、占比和=1（阻断）

### 校验修正（validate.ts 真实缺陷）
- **分区中柱是通长顶梁的中间支点**：val-002 挠度与 val-001 跨度选型的有效跨度 = min(梁长, 最大列净跨)，消息注明"（分区中柱支承，按最大列跨）"——修复前 1200 宽三列被通长梁 1160 误判挠度 20mm 超限

### golden 第 16 节
1200×400×900 三列（35/30/35：左3抽屉/中开放/右柜门右铰）断言：8 柱、每层 beam-x 3 段×2、3 抽屉盒+3 前脸（全在左列 x<−100）、门在右列 x>200、状态非 invalid。16 节全过。

### UI（PartitionsConfig）
- 每列：宽 %（自动归一）/类型/计数/铰链；>2 列可单列移除；≤4 列可"＋加一列（均分）"
- 两列 centerColumn 面板下加"＋第三列（升级多列分区）"一键转换；"中柱"checkbox 覆盖两种模式
- 浏览器实测截图：左列 2 抽屉带拉手+中列开放+右列带门大柜，顶板盖全+6 脚轮——真实三列工具柜形态 ✓


## 2026-08-19（续23）：2040 × 抽屉/分区组合解锁

**commit 205a146** `feat(engine): unlock 2040 beams with drawers/partitions`

- 内部结构 y 基准泛化：`innerBottomY = bh`（底框梁顶）、`innerClearH = H − 2·bh`（净高）——抽屉塔节距/轨道梁 y、列内搁板节距、中柱顶（`centerTopBeamY − s/2 − 2·beamDrop` = 顶梁底）全部改用；方形梁 bh=s 时退化为原公式，锚点③三抽屉柜不受影响
- 删除"2040+抽屉/中柱阻断"；golden 14 节断言反转：2040+抽屉塔组合可生成、首层轨道梁 y = 底梁顶(40)+20 无碰撞
- 鱼缸架/衣柜锚点：案例库（21号文档）无真实 BOM 素材，诚实跳过（只有 archetypes 档位数据，无从对齐）——留待收集到真实产品 BOM 后再做
- tsc ✓ golden 16 节全过 ✓ 已推送（CI 自动上线）

### 后续清单全部清空 🎉
爆炸联动/门开向/前脸/五金/板缝/三列分区/2040组合/STEP命名/锁定解锁/挂钩/M5回归/装配硬件/部署上线——今日全部完成。
